# Japan Macro / BOJ Hawk-Dove Dashboard — project notes

## G7 real wage growth comparison chart added, Markets & bonds tab (2026-08-22)

Same session as the G7 CPI chart above — HanHan had originally forwarded two screenshots (real wage growth first, CPI second), but only the CPI one got actioned. She caught the gap ("Where is the real wage chart?") and confirmed building it. New full-width card, `#cG7Wage`, added right after the G7 CPI card, same layout/palette convention.

**Verified the reference screenshot's "Japan ~1.7% early 2026" claim against primary sources rather than copying it** — researched via a background agent hitting each country's statistical agency directly. Japan doesn't hit exactly 1.7% on MHLW's standard real-wage deflator (CPI ex-imputed-rent) for any single month, but MHLW's alternate CPI-total-deflated figure is exactly +1.7% for Jun 2026 — almost certainly what the source screenshot used. Either way, directionally and in-range confirmed: Japan real wages ran +1.4% to +2.0% Feb-Jun 2026, the best stretch since the 2022-24 inflation shock, after 18 straight negative-to-flat months through 2024-25.

**Sourcing is genuinely mixed frequency/methodology across the 7 countries — disclosed explicitly rather than flattened into one consistent-looking line**, hardcoded JS constant `G7_WAGE` (same "not wired into `fetch_market.py`" pattern as `G7_CPI`, reuses its `labels` array):
- **Japan**: MHLW 毎月勤労統計調査, official real wage index yoy — directly published, monthly, standard CPI-ex-imputed-rent deflator, 確報 (final) used over 速報 (flash) where available.
- **US**: BLS Real Average Hourly Earnings (CES0500000032) — directly published, monthly. Real permanent gap at Oct 2025 (BLS confirmed the underlying CPI reference data was permanently lost to that month's government shutdown, not just delayed — same root cause as the CPI chart's Oct 2025 gap, but here it's unrecoverable, not just late). Real wage growth clearly deteriorated through spring/summer 2026 as CPI reaccelerated (+1.4% Feb 2026 → -0.7% May 2026).
- **UK**: ONS X09 dataset, real regular pay — directly published, but it's a 3-month rolling yoy figure (dated by the window's last month), not single-month like the others; flagged explicitly in the note. Ends Apr 2026, pending release.
- **Canada**: no official real-wage index exists (confirmed via StatCan and Bank of Canada's own "wages and costs" dashboard) — **computed here** as StatCan LFS nominal wage growth minus the CPI figure already used in the `G7_CPI` chart, so the two charts are internally consistent.
- **France**: INSEE SMPT réel — directly published but quarterly only, plotted at quarter-end months (Mar/Jun/Sep/Dec) with `pointRadius:3` dots since a dashed line alone wouldn't show discrete quarterly readings clearly. INSEE's own Q2 2026 figure is a forecast (-0.5%), not an outturn — excluded rather than plotted as if it were actual.
- **Germany**: Destatis Reallohnindex — directly published, quarterly, same quarter-end plotting convention as France.
- **Italy**: no comparable official quarterly/monthly series exists — only one usable data point found (OECD Employment Outlook's Q1 2026 country-note figure, +1.3%), plotted as a single dot (`pointRadius:4`) rather than force-fitting a fake trend line through one point. A computed ISTAT-nominal-minus-CPI proxy exists but uses a narrower wage concept not methodologically comparable to the rest of the chart — deliberately not charted, flagged in the note as low-confidence instead.

Verified live via `agent-browser` on localhost:8501 (Markets & bonds tab): 7-line chart renders correctly below the CPI chart, Japan bold-red visibly dipping negative through 2024-25 then rising into 2026, quarterly dashed lines with visible dots for France/Italy/Germany, zero console errors (`get_space_errors` clean). `<div>` 601/601 and `{`/`}` 2232/2232 balanced before/after. `node --check` on the extracted `<script>` block passed. Checkpoint-committed before the edit and committed again after, per this project's standing large-edit safety rule.

## G7 headline CPI comparison chart added, Markets & bonds tab (2026-08-22)

HanHan forwarded an X screenshot of a "図1 消費者物価上昇率(総合)" chart (US/Canada/UK/France/Italy/Germany/Japan headline CPI yoy, 2024-2026, Japan highlighted, ending 1.9%) and asked to replicate it on "the Japan website." New full-width card, `#cG7Cpi`, added to the Markets & bonds tab right after the two existing BOJ CPI cards (ex-fresh-food / distribution measures) — same insertion point content-wise, since that's where this dashboard's other inflation charts already live (not the "Dashboard" tab itself, which is just KPIs/rate-path/tone/speakers).

**Data, one-off hardcoded JS constant `G7_CPI`** (not wired into `fetch_market.py` — a 7-country comparison doesn't fit that single-series pipeline pattern, same as the Taiwan/FXR-style static charts on sibling dashboards):
- **Japan**: Statistics Bureau's own `zenkoku.pdf` (stat.go.jp), Table 3-1 (総合・前年同月比の推移) — pulled the actual PDF and parsed the official monthly table directly (not a secondary aggregator), confirming Jul 2026 = 1.9%, matching the screenshot exactly and the Reuters/CNBC same-day coverage of that print.
- **US/UK/Canada/Germany**: OECD's live SDMX API (`OECD.SDD.TPS,DSD_PRICES@DF_PRICES_ALL,1.0`, GY/growth-rate transformation, national CPI NSA) — found this endpoint after discovering FRED's own OECD-mirror series for these countries (`JPNCPIALLMINMEI`, `GBRCPIALLMINMEI`, `CANCPIALLMINMEI`, etc.) are all dead/discontinued (stopped updating 2022-2025) — a real trap, don't reuse those FRED series IDs for this kind of chart again.
- **France/Italy**: OECD's own GY series for these two stops at Dec 2025, so computed yoy directly from Eurostat's HICP index levels (FRED `CP0000FRM086NEST`/`CP0000ITM086NEST`, live through Jul 2026) — cross-checked this self-computed method against Germany's own OECD-reported GY figure first (within 0.05-0.5pp every month) before trusting it for two full countries with no independent check.
- US has a real gap at Oct 2025 (delayed BLS release during that month's government shutdown, confirmed via a second independent source, not a fetch bug); UK/Canada both end at Jun 2026 (their Jul prints aren't out yet as of this build). All disclosed in the card's own note rather than smoothed over.

**Real bug hit and fixed during the build**: first version used `responsive:true, maintainAspectRatio:false` with just `max-height` on the bare `<canvas>` (no wrapping div with an explicit height) — Chart.js computed a 0×0 canvas because the tab this card lives on (Markets & bonds) isn't the default-active tab, so the card was `display:none` at chart-creation time. Fixed the immediate cause (wrapped the canvas in `<div style="height:360px">`, matching the working `chart-jgb-funds` pattern elsewhere in this same file) — but the deeper cause was the hidden tab panel itself, not the missing wrapper div. Confirmed this dashboard already has a generic fix for exactly this class of bug: `showTab()` calls `.resize()` on every `Chart.getChart(canvas)` instance inside the tab being switched to, which recovers any chart built while its panel was hidden — so no per-chart special-casing was needed, just relied on that existing mechanism (verified live: chart is 0×0 immediately after page load, then renders correctly the instant "Markets & bonds" is clicked).

Verified live via `agent-browser` on localhost:8501 (clicked into Markets & bonds tab, confirmed the 7-line chart renders with Japan bold-red and ending at 1.9%, zero console errors) and on the public URL via `curl` (200, new card content present in the response body). `<div>` 598/598 and `{`/`}` 2202/2202 balanced before/after. Checkpoint-committed before the edit and committed again after, per this project's standing large-edit safety rule.

## Medium/Long/Super-Long "flows by players" — 3-panel report chart (2026-08-16)

HanHan sent a photo of this dashboard's live "Super long-end flows by players" chart (Cross-border flows tab — monthly net buying of >10y JGBs by Trust banks/Foreigners/Life insurers, JSDA source) and asked to mimic it for Medium-Term and Long-Term too, by the biggest buyers in each bucket, preview-only first. Then: "This is for report so make it professional" and "Can you make these three charts side by side."

**No new data pull** — `fetch_jsda_superlong.py` already computes a per-player tenor split (medium/long/superlong) in `superlong_data.json`'s `byplayer_tenor` object; it just wasn't charted for medium/long yet. "Biggest buyers" = top 3 by cumulative net buying (Apr 2021–latest) per bucket — this selection rule reproduces the live chart's own Trust/Foreign/Life picks for the superlong bucket exactly, confirming it's consistent with what's already published. Medium-term's top 3: Foreigners (dominant, classic hedged-carry maturity), Trust banks, Regional banks. Long-term's top 3: Trust banks, City (mega) banks, Regional banks — foreigners barely register; mega banks show up as a real buyer here despite being net sellers of super-long paper.

Built as one 3-panel figure (`jgb_flows_by_players_3panel.png`, script `preview_flows_by_tenor.py`), white-background report style matching this workspace's other report exhibits (Nomura framework chart, driver-decomposition chart) rather than the dashboard's dark house CSS — same institution gets the same color/linestyle across all 3 panels (Trust=blue solid, Foreign=red dashed, Life=green, Mega=gold, Regional=purple dashed) so cross-panel comparison reads cleanly, one shared legend below all three. **Preview only, not pushed to the live site** — saved as `preview_flows_by_players_3panel.png` / `preview_flows_by_tenor.py` in this project dir; HanHan's call on whether/where it goes live (natural fit: next to the existing super-long card on the Cross-border flows tab, replacing single-panel with this 3-up).

## NISA tab KPI row replaced with two FSA tables, ported from japan-nisa-tracker (2026-08-15)

HanHan: "Change everything in Japan dashboard, nisa website is only for review" — right after the same table redesign shipped on `japan-nisa-tracker`'s Overview tab (see that project's own AGENTS.md entry same day). Confirms the established pattern this week: NISA-related builds get tested/reviewed on the standalone `japan-nisa-tracker` site first, then the finished version ships here — **this dashboard (`boj-hawk-dove-assessment`, "Japan dashboard"/"Japan website") is the one HanHan actually treats as live**, not the NISA tracker.

Ported verbatim: the NISA tab's `#nisaKpis` 5-tile KPI row (cumulative buys ¥71tn, accounts, 10-firm monthly buys, growth-quota share, "41% into domestic stocks") replaced with the same two tables built on `japan-nisa-tracker` — "1. Size and type of assets under NISA program" (`NISA_SIZE_BY_TYPE`, FSA product-type flow/balance, matches a colleague's own reference email to the cent) and "2. Domestic vs. foreign split" (`NISA_DOM_FOR_SPLIT`, 91.4%/8.6% flow and 90.0%/10.0% balance for listed equities, sourced from FSA's raw `2512.xlsx` workbook, growth-quota 上場株式 rows). Same `.fsaTable` CSS (navy/gold/blue) added to this project's stylesheet. New `buildNisaSizeByType()` function, called from `buildNisaTab()` in place of the deleted KPI-tile `innerHTML` write — `cumL`/`accL`/`monL`/`alloc`/`yoyCum`/`yoyAcc` are all still computed and still drive the target box, alloc box, and quota donut charts further down the tab, only the KPI-tile display itself was removed. Also repointed the `nisa_data.json` fetch-failure fallback (previously wrote into the now-deleted `#nisaKpis`) to `#nisaSizeByTypeNote` instead.

See `japan-nisa-tracker/AGENTS.md` for the full sourcing detail (exact FSA cell references, the 41%-vs-91.4% reconciliation for the JSDA figure elsewhere on this tab) — not re-derived here, this was a straight port of already-verified numbers/logic.

Verified live via `agent-browser` on localhost:8501 → NISA tab: both tables render with correct data, all downstream charts (monthly seasonality, cumulative YTD, top-20 fund tracker) unaffected, zero console errors. Committed per this project's standing large-edit safety rule.

## Long-JGB mutual fund tracker added (2026-08-15)

HanHan: "Is it possible to start tracking mutual fund that have jgb as underlying... I would start tracking the AUM and monthly inflows of MUFG, Daiwa and Amova's long-JGB funds." Researched feasibility first (background agent), then confirmed she wanted it on this dashboard's JGB Ownership & Retail tab.

**Funds tracked** (one flagship long/super-long JGB fund per manager, confirmed genuine via research — not guessed):
- **MUFG**: 日本超長期国債インデックスファンド（ラップ向け）, fcode `0331320C`. Wrap-channel only (discretionary investment wrap accounts, not sold directly to retail) — MUFG's own retail long-JGB lineup is thin/nonexistent by comparison; its actual "超長期日本国債ファンド（2046年満期・限定追加型）" hasn't even launched yet (設定日 2026-09-24, still mid-subscription as of this build) so had no data to pull.
- **Daiwa**: iFreeHOLD 日本国債（JGB2056）, fcode `04311258`. ~30yr target-maturity, NISA growth-quota eligible, launched 2025-08-21. Daiwa runs the deepest long-JGB lineup of the three (also JGB2045 etc.) — this is its largest.
- **Amova**: Tracers 日本国債ウルトラロング（30年平均）年4回分配型, fcode `0231225B`. 27-33yr residual-maturity JGBs, launched 2025-11-26. Amova = the 2025 rebrand of Nikko Asset Management's JV.

**Data source — Yahoo Finance Japan is geo-blocked from this sandbox** (confirmed: every `finance.yahoo.co.jp/quote/*` fetch, via curl, `read_webpage`, and `agent-browser`, redirects to Yahoo's "not available in EEA/UK" notice page — a genuine IP-geolocation false-positive on this sandbox's egress, not a wrong-code issue). **Nikkei's per-fund page** (`nikkei.com/nkd/fund?fcode=<code>`) has no such block and carries the same two fields: 純資産総額 (AUM, ¥100mn/億円) and 資金流出入（1ヶ月）(net 1-month flow, ¥100mn). Plain `requests`/`urllib` get redirected to a login wall; `curl_cffi` with Chrome impersonation (`impersonate="chrome"`) gets through cleanly — confirmed this is the same mechanism `read_webpage` itself uses under the hood.

**Real, disclosed limitation — flow has no history anywhere.** Checked Nikkei, Minkabu, Yahoo Finance Japan, and each manager's own site: every one of them shows monthly net flow as a live "last 1 month" snapshot only, never a historical series. AUM itself does have genuine daily history (Nikkei's own `/history` sub-page) but wasn't pulled for v1 — the harder, newer ask was flow, and building a from-scratch AUM history without matching flow history seemed lower value than shipping both fields forward-tracked together. So `jgb_funds.json`'s `history` array can only grow one row per future run of `fetch_jgb_funds.py` — there was no way to backfill it, and the card's own note says so rather than presenting a single point as if it were a real time series.

New standalone script `fetch_jgb_funds.py` (same "slow-moving series, not wired into `fetch_market.py`'s daily `SOURCES`" pattern as `fetch_retail_jgb.py`) — regex-parses each fund's Nikkei page (`m-stockInfo_detail_value` CSS-class-anchored, not markdown-structure-anchored, since this script fetches raw HTML directly rather than through `read_webpage`), converts 百万円↔億円 to a consistent ¥100mn (億円) unit, and appends one row per run to `history` (dedup by date, so re-running same-day overwrites rather than duplicates). Output `jgb_funds.json`.

New card on the **JGB Ownership & Retail** tab, right after Retail JGB demand — 3 KPI tiles (AUM + latest monthly flow per manager, colored up/down by flow sign) plus a 3-line AUM chart (currently a single point each, will build into real trend lines as the script re-runs). Verified live via `agent-browser`: KPIs and chart render with real non-zero data, no console errors; confirmed on the public URL via `curl` (both the page and `jgb_funds.json` itself).

## New "JGB Ownership & Retail" tab split out of "Auctions & fiscal" (2026-08-15)

HanHan: "Can you separate our retail JGB chart and household balance sheet etc to a separate tab before seasonality tab as it's too much info in the fiscal tab." The "Auctions & fiscal" tab had grown to 7 cards mixing two different topics: issuance/supply-side (MOF auction calendar, Japan fiscal watchlist, JGB gross issuance plan by maturity) and holdings/demand-side (JGB holdings by holder type — snapshot + multi-year-shift, household financial assets balance sheet, retail JGB monthly subscriptions).

Split the 4 demand-side cards (JGB holdings snapshot, JGB holdings multi-year shift, household financial assets, retail JGB demand) into a new tab, **`tab-ownership`** / nav label "JGB Ownership & Retail", inserted in the tab bar right after "Auctions & fiscal" and before "Seasonality" per HanHan's exact placement ask. "Auctions & fiscal" now holds just the 3 supply-side cards (calendar, fiscal watchlist, issuance plan). Pure HTML reorg — no JS/chart-builder functions touched (`buildJgbHolders`, `buildHouseholdAssets`, `buildRetailJgb`-equivalent code all build eagerly on page load regardless of which panel is visible, confirmed via `showTab()`'s generic `#tab-<id> canvas` resize-on-activate logic, so moving the `<div>`s was safe). Verified live via `agent-browser`: nav order correct, new tab's 4 canvases render with real data, Auctions & fiscal down to 3 cards, no console/service errors, confirmed on the public URL via `curl`. Checkpoint/commit taken per this project's standing large-edit safety rule (file was clean pre-edit, single commit after).

## NISA report to Angela — v3 correction pass, same day (2026-08-13, report-only, no dashboard change)

HanHan sent a second, more rigorous review file ("nisa_v3_final_review_corrections_2026-08-13...txt") of the just-sent v3 report, flagging 1 major analytical construction error and 3 statistical mislabelings, all "must fix before distribution" — except v3 had already been distributed to Angela by the time this arrived. Corrected and re-sent same day as a follow-up "Correction:" email (clearly framed as superseding the earlier send, itemized what changed and why, not just a silent replacement):

1. **Major — the ¥165tn/2.5% "Retail JGB Absorption Ratio" was a genuine construction error, not just a calibration nit.** Numerator (household JGB holdings net *change in stock*, +¥4.18tn) and denominator (gross issuance minus BOJ net purchases, ≈¥165tn — where gross issuance includes refinancing/rollover of maturing JGBs, not a net incremental-stock figure) weren't conceptually matched. Removed the precise ratio and the ¥165tn "absorption requirement" derived from it everywhere it appeared (exec summary Rates row, Section 6's dedicated subsection, the scenario-table comparison paragraph, Section 8's JGB row) — kept the three raw component figures (household net change, gross issuance, BOJ net change), each individually labeled with what it actually measures, and reframed Section 6's subsection to explain *why* the ratio was wrong rather than silently dropping it. The qualitative conclusion survived unchanged (JGB-NISA helps marginal absorption, doesn't solve the structural redistribution problem) — this was a quantitative-calibration issue, not a directional one.
2. **74.9% "new-NISA funding" reworded** — JSDA's survey stat is % of respondents citing a funding source (multiple responses allowed), not a yen-value share; was incorrectly presented as if it were a flow decomposition.
3. **Kodomo NISA ¥600k corrected from "lifetime cap" to "annual investment limit"** — the actual lifetime/tax-exempt-holding limit is ¥6mn, a materially different number that was simply wrong in v3.
4. **48.8% domestic-equity stat reworded** (exec summary + Section 8) from "purchase value" to "reported purchased security names" — same JSDA investor-survey caveat as #2, a respondent/name-count statistic, not yen-weighted.
5. **Nice-to-fix: added Junior NISA's ¥0.8mn/yr cap**, which the pre-2024 comparison table's own row had omitted despite the table listing Junior NISA as one of the three schemes being compared.

Built as `email_body_v4.html`/`full_report_v4.html` (badge text "v3 (corrected 13 Aug 2026)" — same v3 content, corrected, not a new v4 analytical pass) via targeted edits to a copy of the v3 HTML, not a rewrite. PDF regenerated via weasyprint, verified via `pdftoppm` screenshots of pages 1 (exec summary) and 7 (Section 6's corrected subsection) before sending — both render cleanly. Sent as a new email thread (not a reply) with a red-bannered "Correction to the v3 report sent earlier today" summary up top itemizing exactly what changed and why, followed by the full corrected report inline, PDF attached. Verified attachment via the standard `gmail-get-message` byte-size check (796,641 bytes ≈ 552.7KB PDF × 1.37 + ~35KB HTML body). PDF hosted temporarily via `zopub sync nisa-report-v3-corrected-2026-08`, removed after send since nothing references it inline (unlike the chart-image collection, which stayed the same across this correction — no charts changed, only text/tables).

**Lesson for future report builds in this workspace:** a numerator/denominator mismatch (net stock change ÷ a gross-flow-derived figure) produced a precise-looking ratio (2.5%) that read as far more rigorous than it was — worth deliberately stress-testing any newly-constructed ratio's numerator and denominator for consistent units/basis (net vs. gross, stock vs. flow) before presenting it as a headline number, not just sourcing each side independently and assuming compatibility.

## NISA report to Angela — v3 rework per external review critique (2026-08-13, report-only, no dashboard change)

HanHan sent a 13-point critique file ("nisa_v2_review_suggestions_2026-08-13.txt") of the v2 Angela report and asked to "do another attempt incorporating some suggestions here." Built v3 (`email_body_v3.html`, PDF `NISA_v3_Household_Flow_JGB_Absorption_Framework_2026-08-13.pdf`, saved to `Documents/Japan NISA/`) implementing the critique's 8 priority changes:

1. Reframed the JGB-NISA passage probability (previously a flat 60-70%) as a 5-stage trigger ladder (DPP proposal → LDP Tax Commission review → FSA/MOF request → ruling-party tax outline → legislation) with lowered, hedged point estimates (~50-60% odds of *some* JGB/bond-NISA reform by FY2029; <30% odds the DPP bill passes substantially as filed).
2. Built a **Retail JGB Absorption Ratio** (household JGB net purchases ÷ incremental private-sector JGB absorption requirement) from this workspace's own primary data — BOJ Flow of Funds household JGB stock (+¥4.18tn, Mar-2025→Mar-2026), MOF gross JGB issuance (¥115.5tn trailing 12mo), and `boj_balance_sheet.json`'s own BOJ JGB net-change series (−¥49.5tn trailing 12mo) → absorption requirement ≈¥165tn → ratio ≈2.5%. Full working shown in the report so it can be independently re-derived.
3. Replaced the single JGB-NISA flow guess with a 4-scenario table (existing-allowance ~¥0.5-1.5tn / separate allowance ~¥1-3tn / strong dormant-account adoption ¥3tn+ / explicit long-duration incentives, uncertain), each compared against the ¥165tn absorption requirement rather than total JGB outstanding.
4. Softened the JPY conclusion to be conditional on what NISA money gets displaced (foreign-fund substitution → JPY-positive; domestic-equity or deposit substitution → minimal FX effect).
5. Added a household portfolio-beta / BOJ-transmission section — NISA structurally raising household wealth's sensitivity to asset prices, a stronger consumption/confidence transmission channel than a decade ago when >50% of household assets sat in cash.
6. Added an explicit "cumulative purchases ≠ current NISA AUM ≠ net new household saving" caveat (no single source reconciles all three; flagged as a gap, not estimated over).
7. Put "NOT NISA-specific" directly on the Wealth Advisor top-20 fund-flow chart title/caption (retail fund-flow proxy, not an NISA-account series) and lowered confidence on the gold/diversification read for the same reason.
8. Rebuilt the executive summary as a Rates/FX/Domestic equities/Global equities/Policy/JGB curve table instead of prose.

Sent to Angela (cc HanHan per standing rule) in two emails: HTML body + report content first, then a follow-up with the PDF correctly attached (first attempt mistakenly attached a chart PNG under a `placeholder.png` filename instead of the PDF — caught via the routine `gmail-get-message` byte-size sanity check, 756,961 bytes didn't match a chart-PNG-sized attachment, re-sent correctly and reverified: 756,961 bytes ≈ 552,166-byte PDF × 1.37 base64 overhead). PDF hosted temporarily via `zopub sync nisa-report-v3-pdf-2026-08`, removed after send (`zopub rm`) since it wasn't referenced inline anywhere. **Chart-image collection `nisa-report-v3-2026-08` stays live** — it's referenced inline by the sent v3 email body, same rule as the v1/v2 chart collections.

Report-only pass — no `index.html` or `nisa_data.json` changes this round, unlike the two entries below.

## NISA tab — Mar-2025 cumulative-purchase gap backfilled + 10-firm chart converted to seasonality (2026-08-13)

HanHan, reviewing the Angela report: "In your first chart March 2025 is missing." Real data gap, not a rendering bug — `whole_market_fsa.cumulative_purchase_jpy_tn` had entries for 2024-12/2025-06/2025-12 but skipped 2025-03, even though `accounts_10k` DID have a 2025-03 entry (26.46mn) from the same FSA release. Researched and confirmed: FSA/JSDA published a real ad hoc March-2025 cumulative-purchase figure (¥59.3tn all financial institutions) paired with that same 26.469mn account count — JSDA reference chart `250528shiryou5.pdf` (published 2025-05-28) explicitly labels a "2025年３月末" column, corroborated independently by 日本総研 (JRI, 2025-04-14) and secondary press (Money Palette, PR TIMES) all citing "2025年3月末で累計59兆円." Passes sanity check sitting between the existing Dec-2024 (¥53tn) and Jun-2025 (¥63tn) points. Backfilled `{"date":"2025-03","value":59}` into `cumulative_purchase_jpy_tn` in **`japan-nisa-tracker/nisa_data.json`** (the canonical source `fetch_nisa.py` writes to) and copied to the `boj-hawk-dove-assessment/nisa_data.json` mirror, matching the existing mirror pattern. Verified live via `agent-browser`: `nisaCumChart` line + KPI row unaffected (YoY calc anchors on 2024-12, `last()` still resolves to Dec-2025), zero console errors. Also regenerated the standalone report chart (`nisa_report/build_charts.py` in the conversation workspace) — the March-2025 bar that was previously blank in "Chart 1: NISA cumulative purchases & account growth" now shows ¥59tn.

**Same day, "10-firm monthly gross buys" bar chart converted to a seasonality line chart** per HanHan's ask ("instead of showing bar chart, maybe we should convert it to seasonality chart?") — this is a straight replacement, not an addition alongside (contrast with the separate "Cumulative NISA buys — YTD by year" card right below it, which stays a cumulative-within-year line chart, a different view). New `nisaMonChart` (same canvas id reused) now plots one line per calendar year (2024 grey/partial Jan-Sep, 2025 blue/full year, 2026 red/bolded/partial Jan-May) on a Jan-Dec x-axis, `spanGaps:false` so partial years show as real breaks — identical convention and color palette (`{2024:"#8a94ad",2025:"#4a7fc0",2026:"#e2564a"}`) to `japan-nisa-tracker/index.html`'s own `buildMonSeasChart()`/`monSeasChart`, which is the established house pattern for exactly this "same series as small-multiple by year" transform (also used for BOK's KTB tenor-seasonality tiles). Card retitled "10-firm monthly gross buys — seasonality"; note dynamically states which years are partial and why. Verified live via `agent-browser`: January-spike-then-spring-fade pattern clearly visible across all 3 years, zero console errors, KPI row and neighboring YTD-cumulative chart unaffected.

## NISA tab — audit corrections applied (2026-08-13)

HanHan sent a full external audit of the NISA tab ("boj_dashboard_nisa_tab_full_audit_2026-08-13.txt") flagging one material factual error plus several loose-labeling issues, then confirmed "Yes can you fix it on dashboard also" after I'd already applied the same corrections to a written report sent to Angela. Applied the audit's "DO NOW" priority-1 list (7 items) to `index.html`:

1. **Material error fixed** — deleted the false claim that growth-quota direct-stock buys are "mechanically ~100% domestic since NISA doesn't permit direct foreign-stock purchases." Foreign-listed stocks/ETFs ARE eligible in the growth quota; JSDA's 2025 NISA White Paper shows 9 of the top 10 growth-quota stock names in 2025 were domestic, 1 foreign — heavily domestic-tilted, not mechanically 100%. Fixed in the "classified" chart's note (renamed from "Individual funds, classified" to "Retail fund-flow proxy, classified — domestic vs. foreign equity mandate").
2. **FSA cadence** — opening description changed from "quarterly" to "generally semiannual (Jun-end and Dec-end releases, Mar-2025 was an ad hoc exception)."
3. **Wealth Advisor relabeled** as "Retail fund-flow proxy — NISA-sensitive, not NISA-exclusive" (both the snapshot card and the time-series card) — it measures total fund flow including taxable/iDeCo purchases, not NISA-account-specific flow.
4. **Top-20 1M/3M/6M/1Y methodology** — title now dynamically reads "Latest-month top-20 retail funds — ranked by [net monthly / trailing 3M / 6M / 1Y] flow," and the note explicitly states the universe is fixed to the latest month's 1M top-20 — trailing windows only re-rank/re-size that same fixed set, they do NOT rebuild the top-20 for that window (so a fund large 6-12mo ago that's since dropped out won't appear even on 6M/1Y views).
5. **"Top 20 = biggest slice of NISA-driven demand" softened** to "captures an important concentration of retail fund demand in products commonly used through NISA, but this is neither exhaustive nor NISA-exclusive."
6. **MoF toshin outward-flow card relabeled** — "Macro proxy, not a direct NISA FX series," explicitly says not to convert it into an implied NISA-specific FX-buying number.
7. **Quota-split cards retitled** "Quota split — JSDA 10-firm, 2026 Q1" and "Within the growth quota — JSDA 10-firm, 2026 Q1" (previously bare "2026 Q1," ambiguous vs FSA whole-market) — also flagged in the growth-quota note that it's a two-way stocks-vs-funds split only, doesn't yet separate direct securities into domestic vs foreign.

**New: Policy Watch — JGBs and NISA box**, added right below the tab's opening description (before the KPI row) — gold-accented callout stating current law plainly (JGBs are NOT NISA-eligible under either quota per JSDA's FAQ), then walking the JGB-NISA proposal through explicit status tags (DISCUSSION → BILL [opposition DPP, 10 Jul 2026, not LDP/government] → Katayama's warming tone / LDP tax-commission review, but realistic track is FY2027-29 tax reform, not the DPP bill passing as-is since the ruling coalition lacks an Upper House majority), flags Kodomo NISA as a separate already-confirmed Jan-2027 change not to conflate with JGB-NISA, and states the unresolved design question (would JGB purchases consume the existing ¥3.6mn allowance — substitution risk — or a separate allowance — incremental demand). Sourced from the same research used for the Angela report same day (DPP/Jiji/NRI/Zaikyo reporting through 22 Jul 2026).

**Not done this pass** (audit's "ADD NEXT" tier — bigger new-research/new-chart lifts, not simple text/label fixes): JSDA domestic-vs-foreign direct-stock series as a new dataset, estimated overall NISA foreign-allocation range panel, hedged-vs-unhedged foreign exposure, full reorder of the tab into the audit's 5-section structure, data-quality badges (official/survey/proxy/modelled) on every chart. Flag to HanHan if she wants these prioritized next — see the audit file itself (in her Telegram attachment) for the full item list and priority tiers.

Verified live via `agent-browser` on `localhost:8501`: Policy Watch box renders, both quota-split cards show the new titles, zero console errors, all charts confirmed actually rendering via `canvas.toDataURL()` non-trivial byte-length checks (the full-page screenshot itself showed several charts as blank white space — this is the documented, known screenshot-artifact for canvas content in this environment, not a real regression; same workaround as this file's other entries). `<div>`/`</div>` count balanced (536/536) before and after.

## Cumulative YTD-by-year NISA buys chart moved here from japan-nisa-tracker (2026-08-12)

HanHan asked for a JPM-style "Figure 6" cumulative-¥tn-by-year NISA flow chart; it was built on `japan-nisa-tracker`'s Monthly Pulse tab first. She then asked "Where is this saved. It should be in nisa tab" — same consolidation preference already established for the Top-20 fund flow tracker (moved here 2026-08-11, see below): NISA fund-flow visuals belong on this dashboard's NISA tab, not the separate japan-nisa-tracker site.

Moved the chart (`nisaCumYtdChart`) into `buildNisaTab()`, placed right after "10-firm monthly gross buys" (same relative position it had on the source project's Pulse tab) — same computation, client-side cumulation within each calendar year from the already-loaded `nisaD.ten_firms_jsda.monthly_purchase_jpy_100m.series` (no new fetch; that data was already loaded here for the KPI row and `nisaMonChart`). Removed the equivalent card + JS from `japan-nisa-tracker/index.html`'s Pulse tab, same pattern as the earlier Top-20 tracker move — the underlying `nisa_data.json` pipeline (`fetch_nisa.py`) is unchanged, still mirrors into this project automatically. Verified live via `agent-browser` on both `localhost:8501` (chart renders, KPIs intact, zero console errors) and `localhost:8504` (card + canvas gone from Pulse tab, no orphaned canvas, zero console errors).

## News tab removed (2026-08-12)

HanHan: "We can remove Japan News tab." Removed the tab button, its `#tab-news` panel (headlines list + tag filter buttons), and all supporting JS (`TAG_LABELS`, `_allNews`, `renderNews()`, `paintNews()`, the `renderNews(M)` call in the market_data.json load chain, and the `newsList` fallback text on fetch failure) plus the orphaned `.news-list`/`.ni`/`.ntag*`/`.news-filters`/`.nf` CSS rules from `index.html`. **Left the underlying data pull alone** — `fetch_market.py`'s `fetch_news()` (Google News RSS, EN + JA queries) still runs and still writes `market_data.json → news`, since `validate_data.py` checks for it and nothing else on the dashboard depends on removing it; if HanHan later wants the fetch itself stopped too, that's a separate follow-up (would also need to relax `validate_data.py`'s news-emptiness check). Checkpointed to git before the edit (standing safety-net rule for large `index.html` edits) and committed after. Verified live via `agent-browser` on the public URL: News button gone from the nav, all other tabs intact, zero console errors.

## NISA tab — USD toggle added to the fund-flow time-series chart (2026-08-12)

HanHan: "For Japan nisa tab. Can I have USD option toggle as well" — the NISA tab's "Top 20 funds" snapshot chart already had a ¥/$ toggle (`nisaFundFlowCcyJpy`/`nisaFundFlowCcyUsd`, built 2026-08-11 alongside the fund-flow tracker itself); the "Top NISA funds — net monthly inflow over time" chart directly below it (same underlying Wealth Advisor data, just plotted as a time series instead of a single-month ranking) didn't have one.

Added a second toggle button pair (`nisaFundFlowTSCcyJpy`/`nisaFundFlowTSCcyUsd`) to that chart's header, wired to the **same shared `nisaFundFlowCcy` state** as the snapshot chart rather than an independent one — clicking either toggle updates both button pairs and redraws both charts, so the whole NISA fund-flow section stays in one currency. `buildNisaFundFlowTSChart()` gained a `conv()` helper (divides by `nisaFxRate` when `nisaFundFlowCcy==='usd'`) applied to all three series (top-20 total, the 6 persistent-fund lines, and the Investment Trusts Association ground-truth line) plus updated axis label/tooltip formatting (`$ bn` vs `¥ bn`, 2 vs 1 decimal places — same convention as the snapshot chart). Reused the already-fetched `nisaFxRate` (loaded async from `market_data.json` when the snapshot chart first builds) rather than a second FX fetch.

Verified live via `agent-browser` on `localhost:8501`: toggling either button pair switches both charts to USD and back, axis/tooltip units update correctly, both button pairs stay visually in sync, zero console errors. Confirmed on the public URL via `curl`. Committed to git.

## JGB gross issuance plan by maturity, FY2026 vs FY2027E — added 2026-08-12

HanHan sent a photo of a Nomura research table ("An estimate of JGB market issuance plan", Source: MOF, Nomura) — a monthly grid by tenor/bucket for FY2026 initial and FY2027 initial. Flagged first (same category as the standing Bloomberg-terminal rule — work-related proprietary broker access, shouldn't land on a public dashboard unlabeled); she confirmed "let's include fy 27 and put Nomura estimate", i.e. proceed for this instance with clear "Nomura estimate" attribution rather than treating it as public MOF data.

New card at the bottom of the **Auctions & fiscal** tab (`#tab-auctions`, visible/live — not the hidden BOJ Balance Sheet tab), grouped bar chart (`cJgbIssuancePlan`) comparing FY2026 (MOF's own plan, post-March-2026 PD meeting) vs FY2027E (Nomura's own forward estimate, not yet MOF's published plan) across 10 buckets: 40y/30y/20y/10y/5y/2y/TB(1y+6m, shown annual since MOF/Nomura convention concentrates TB issuance in the March row)/10y JGBi/Liquidity enhancement (sum of the note's 4 LE sub-columns: 11-39y/5-11y/1-5y×2)/CT (Climate Transition GX bonds, 10y+5y). Only the **annual total row** was transcribed and charted, not the full monthly grid — the monthly sub-row structure in the photo (2 LE auctions/month, alternating 40y months) was reconstructable but risked transcription error for a public page; annual totals were instead verified component-by-component (every tenor, 10yJGBi, and all 4 LE sub-columns sum exactly to the published FY total) before charting, which is a solid accuracy check without needing the monthly detail.

Data is a hardcoded JS object (`jp` in the IIFE at the end of `index.html`'s `<script>` block) — not wired to any fetch script, since it's a one-off note transcription, not a recurring data source. If HanHan wants this to refresh automatically each time MOF/Nomura issue a new note, it would need a proper source (this note has none — it's a manually-shared photo).

**Real bug hit and fixed while building this:** first attempt nested the new chart-building code inside `initGpifChart()` (copied the insertion point from the end of the GPIF chart's IIFE without checking its enclosing function) — that function only runs lazily via `requestAnimationFrame` when the GPIF tab is clicked (`showTab('gpif', ...)` in `index.html`), so the new KPIs/chart silently never rendered on page load or on the Auctions tab. Fixed by extracting the block into its own top-level self-invoking IIFE (with its own local `GRID`/`TICK` consts, since those aren't in scope outside `initGpifChart`) that runs unconditionally at script-parse time — correct, since this chart has no dependency on the async-fetched `D` data. If any other one-off chart needs adding to this dashboard, check which function actually encloses the insertion point before assuming it runs on page load — several charts here are gated behind lazy tab-click (GPIF is one; the AGENTS.md's own NISA-tracker notes mention the same lazy-build pattern was tried on the BOJ side too).

Verified live via `agent-browser` on `localhost:8501` (BOJ dashboard's actual port — not 8502, which is Korea): KPI row (¥168.5tn / ¥174.5tn / +¥6.0tn / +3.6tn TB) and 10-bucket grouped bar chart both render with real data, card sits correctly after the fiscal watchlist table, no other tab/chart affected. Committed to this project's local git repo per the standing large-file-edit safety net.

**Switched from bar chart to a table, 2026-08-12 same day** — HanHan: "I prefer table format" then "Similar to nomura table". Replaced the `cJgbIssuancePlan` canvas/Chart.js bar chart with a plain `<table id="jgbPlanTable">` (uses the dashboard's existing global `table`/`th`/`td` styles, no new CSS) laid out like the Nomura note's own grid — one row per bucket (40y/30y/.../CT), columns FY2026 initial / FY2027 initial / Change, plus a bolded Grand total row; change cells colored hawk-red for increases, dove-blue for decreases. Same underlying `jp` data object (annual totals only, unchanged) — purely a display-format change, no new transcription. KPI row above the table is untouched. Verified live via `agent-browser` (table renders with all 10 rows + total, correct values, zero console errors) and confirmed on the public URL via `curl`. Committed to git.

**Expanded to the full monthly grid, 2026-08-12 same day** — HanHan: "I want this to be done similar to nomura way. By month" (after the table-format switch above, which only had annual totals). This is the monthly transcription the previous entry deliberately skipped as "too risky." Re-read the same photo (still in this conversation's workspace at `telegram_attachment/`) with `imagemagick` crops zoomed to 400-800% per region — the raw photo is too dense/skewed to read reliably at native resolution, several crops were needed to disambiguate two lookalike thin columns (10yJGBi vs. what turned out to be a blank 6mTB) and to work out that the LE section actually has a 4th unlabeled sub-column (a second 1-5y auction in alternating months, header cut off in the photo). **Every monthly figure was cross-validated against the already-correct published FY column totals before use** — didn't just transcribe and hope: derived each column's monthly cadence (40y biMonthly from May, 10yJGBi quarterly May/Aug/Nov/Feb, LE 11-39 alternating 0.90/1.35, CT in May/Aug/Nov/Feb) and confirmed each column's 12-month sum lands exactly on the printed FY total; also reverse-engineered that the table's own "Total" column formula is base-tenors + 40y + 10yJGBi + LE-11-39-only (excludes the two shorter LE buckets) by solving for it against several printed monthly Total values — confirms the transcription is internally consistent, not just eyeballed.

`index.html`'s IIFE rewritten: `jp` (single annual-totals object) replaced with `fy2026`/`fy2027` objects, each a 12-row month-by-month array (14 columns: 40y/30y/20y/10y/5y/2y/1yTB/10yJGBi/LE 11-39/LE 5-11/LE 1-5-combined/CT-combined/Total/Grand total) plus its own FY-total row. HTML swapped the single `#jgbPlanTable` for two stacked tables (`#jgbPlanTable2026`, `#jgbPlanTable2027`), each with its own "FY20XX initial" heading, matching the Nomura note's own two-block layout. Zero values render as "–" to match the source's sparse look; the 1-5y bucket's two sub-auctions and the two CT tranches are combined into one column each for readability (footnoted) rather than reproduced as 4 near-empty extra columns. Verified live via `agent-browser` on `localhost:8501`: both 13-row tables (12 months + FY total) render with figures matching the source photo exactly, KPI row unchanged (¥168.5tn/¥174.5tn/+¥6.0tn/+¥3.6tn), zero console errors. Checkpoint-committed before this edit (per the standing large-rewrite safety net) and committed again after.

Live: boj-hawk-dove-dashboard-hanhan.zocomputer.io (served by `serve_nocache.py`, port 8501 registered service `svc_QqRnJPmCTyQ` — port 8502 is the *Korea* (bok-hawk-dove-assessment) dashboard, don't confuse the two). Reads files in this dir directly — no build step, no server-side cache (headers force `no-store`). Single-page app: `index.html`, tab-switching (`showTab('id', el)`), vanilla JS + Chart.js, dark navy/gold house CSS (`--bg:#0f1522`, `--card:#171f31`, `--accent:#d9a441`) shared across this workspace's BOJ/BOK/China/China-US-AI dashboards.

Architecture: each data domain gets its own `fetch_X.py` script + standalone `X_data.json`/`X.json` file; `index.html` does `fetch('./the_data.json?' + Date.now())` client-side, cache-busted. No shared daily orchestrator exists in this project — each `fetch_*.py` is independently scheduled/run by hand (unlike some sibling projects). New tabs follow the nav-button + `<div id="tab-X" class="panel">` pattern.

## 2026-08-09 — Intervention tab corrections + new BOJ Balance Sheet / QT tab

HanHan shared a 400-line expert-review doc ("Japan Macro / BOJ Hawk-Dove Dashboard Public-Data Expansion Plan + Intervention Tab Audit") with ~20 proposed modules plus Intervention-tab corrections, prioritized Tier 1/2/3. Actioned the two agreed top picks in order, then kept going down Tier 1 as time allowed.

### Intervention tab fixes (`intervention_data.json`, `index.html`)

1. **Episode grouping**: the 2022 "First yen buying since 1998" row (2022-09-22 to 2022-10-24, previously one merged episode) was split into two per the doc's exact historical treatment: **September 2022** (¥2.84tn, MOF-confirmed daily) and **October 2022** (21+24 Oct combined = ¥6.35tn, MOF-confirmed daily). 2022 full-year total = ¥9.19tn ≈ ¥9.2tn, matching the doc's benchmark. Note: the doc's own text says "21/24 Oct combined: ~¥5.62tn" — that's actually just the larger of the two daily figures (21 Oct alone), not the true combined total. The correct MOF/Reuters/Nippon.com-confirmed combined October figure is ¥6.35tn (¥5.62tn on 21 Oct + ¥0.73tn on 24 Oct), which is what this dashboard's `recent_ops` array already had right — used that, not the doc's shorthand, and documented the discrepancy here.
   - All other groupings the doc called out were **already correct** before this session: 2003–Mar 2004 already one campaign (¥292.7bn/-292.7... i.e. one row), March 2011 already separated from Oct-Nov 2011, Golden Week 2024 (¥9.7885tn) and July 2024 (¥5.5348tn, 2024 total ¥15.3233tn) already separate episodes with the right amounts, April-May 2026 (¥11.7349tn) already its own campaign, late-July 2026 already its own separate row. These only got the new status/tag/¥tn fields added below — no number changes, since they already tied out against the doc's benchmarks and against `recent_ops`' daily sums.
2. **`data_status` field** added per episode: `MOF confirmed — daily breakdown` / `MOF confirmed — monthly aggregate` / `BOJ settlement estimate / market estimate` / `Historical estimate` / `Pending MOF confirmation`. Rendered as a colored badge (green/amber/grey/red) in the episode table, not buried in a tooltip. The 30-31 Jul 2026 joint US-Japan operation is explicitly `BOJ settlement estimate / market estimate` (was previously only signaled by a `confirmed:"event"` background tint) and the 23 Jan 2026 rate-check is `Pending MOF confirmation` (no yen was actually transacted, so there's nothing for MOF to confirm as an intervention amount).
3. **`coordination_tag` field** added: `Japan unilateral` / `G7 coordinated` / `US-Japan coordinated`. March 2011 = G7 coordinated (post-earthquake), September 2022 = Japan unilateral (not G20/G7 — a common misconception), 30-31 Jul 2026 and 17 Jun 1998 = US-Japan coordinated.
4. **`japan_jpy_tn`** (actual yen amounts) added alongside the pre-existing USD-only fields. Every USD column/tooltip is now explicitly labeled "Approx. US$bn" or "Approx. Japan US$bn" with a note that MOF only publishes yen — USD is always a derived conversion, never an official figure.
5. **Table restructured** to the doc's suggested column set: Date/episode | Direction | Catalyst | Japan ¥tn | Approx. Japan US$bn | Foreign/coordinated US$bn | Data status | Coordination | 1d effect | 5d effect | 20d effect.
6. **1d/5d/20d effect columns** are newly computed, not curated: for every episode with an exact start date, USD/JPY change vs. the last trading day before the episode's first operation, using the same daily FRED/Yahoo USD/JPY series (`long_history`/`recent_history`) that already backs the long-run chart. Stored per-episode as an `effects` object (`{pre_date, pre_value, d1, d5, d20}`, each with `date`/`value`/`change_yen`/`change_pct`). Recompute by re-running the effects-computation logic if `long_history`/`recent_history` get extended (currently a one-off script, not yet folded into `fetch_intervention.py` — do that if this needs to be a recurring recompute).
7. **"IMF 3x3" language**: checked — the prose never actually said "3x3" (confirmed via grep across all `.html`/`.json`/`.py` files, zero hits), only the card's `<h2>` heading said the vaguer `IMF's "free floating" rule`. Renamed the heading to the doc's exact suggested phrasing: **"IMF free-floating classification criterion — not an intervention constraint"**. Also added an explicit `clarification` field to `imf_rule` in the JSON spelling out that this is a statistical AREAER classification threshold, not a legal/operational cap on intervention.

### New "BOJ Balance Sheet" tab

Positioned in the nav right after "Intervention" (doc's own framing: MOF issuance → BOJ purchases/runoff → private-sector absorption — Balance Sheet sits with the policy/operations tabs, not buried at the end).

**`fetch_boj_balance_sheet.py`** (mirrors `fetch_mof_flows.py`'s house style: dict-of-series output, idempotent overwrite, `meta` block with source/as-of/generated-at) pulls real official data from three sources, no scraping-fragile HTML parsing required for the core series:

- **DB=BS01** "Bank of Japan Accounts" via the **BOJ Time-Series Data Search API** (`https://www.stat-search.boj.or.jp/api/v1/getDataCode`, no API key required, launched Feb 2026 — discovered via the unofficial `bojapi`/`boj-api-client` R/Python packages' docs, not obvious from the BOJ website itself). Clean monthly end-of-month series back to 1998/04, sliced to 2012-01 onward per the doc's ask (175 months as of this write). Fields: total assets, JGS holdings + share of assets, corporate bonds, ETFs, J-REITs, loans, FX assets (asset side); banknotes, current-account deposits, government deposits, repo liabilities, other deposits (liability side).
- **DB=FM05** "Ordinary Government Securities" + "Treasury Discount Bills" Amount Outstanding (same API) — denominator for "BOJ share of total JGB outstanding" (~40-43% latest, lags BS01 by ~1 month).
- **DB=MD06** "Sources of Changes in Current Account Balances and Market Operations" — `MASDM58` (Outright Purchases of JGBs) and `MASDM5A@` (Outright Purchases of T-Bills), monthly since 1960/1999. This is the doc's Section C "JGB purchase operations" ask — a genuine official monthly gross-purchases series (not broken out by maturity bucket; that detail only exists in BOJ's daily operation-result HTML pages at `boj.or.jp/en/statistics/boj/fm/ope/`, which are not a clean scrapable time series and were not built this session).
- **Cabinet Office ESRI** Quarterly Estimates of GDP CSV (nominal, original series) — denominator for total-assets/GDP. Scraped via the EN release-index page (`esri.cao.go.jp/en/sna/sokuhou/sokuhou_top.html`) to find the current release folder, then that release's own menu page to find its actual CSV link — do NOT try to construct the CSV URL from the folder name directly, the file-code suffix doesn't map predictably (e.g. folder `qe261_2` → file `gaku-mg2612.csv`, not `gaku-mg261_2.csv`). Unit is ¥10bn (十億円), divide by 1000 for ¥tn.

Writes `boj_balance_sheet.json`. Derived metrics computed in the fetch script itself (same convention as `fetch_natural_rate.py`, not client-side JS): MoM change in JGB holdings, 12m change, 3-month annualised balance-sheet contraction rate `((assets_t/assets_t-3)^4 - 1)*100`, total assets/GDP (quarterly GDP annualised ×4), BOJ/total-JGB-outstanding share. GDP and FM05 series both lag BS01 by 1-3 months — the `latest` block falls back to the most recent non-null value for those two ratios (with its own `_asof` field) rather than showing null.

Tab content: "why this matters" framing card (doc's own line, verbatim), 6-KPI row, then charts — total BOJ assets since 2012 (line), JGB holdings since 2012 (line), monthly change in JGB holdings (bar), current-account/reserve balances (line), monthly gross JGB purchase operations (stacked bar, JGB+T-Bill), asset composition for the latest month (donut + table), 12-month change in JGB holdings (bar), BOJ share of total JGB outstanding (line), total assets/GDP (line), 3-month annualised contraction rate (bar) — plus a sourcing/caveat footer with links and the exact cross-check note below.

**Verified against the doc's own illustrative 2026 spot-checks** (doc: "BOJ's own real published numbers per the doc's audit, not estimates"):
- 10 Mar ~¥545.6tn — the dashboard's monthly API series shows ¥530.87tn for **end**-March (that's a real end-of-month snapshot, not an error), but the raw acmai HTML release for the exact 10 Mar date (`boj.or.jp/en/statistics/boj/other/acmai/release/2026/ac260310.htm`) shows ¥545,582,587,978 thousand yen = **¥545.58tn**, matching the doc to the tn. Confirms the API series and the acmai page are the same underlying data, just sampled on different days within the month.
- 30 Apr ~¥531.9tn → dashboard: **¥531.949tn** (exact).
- 30 Jun ~¥518.3tn → dashboard: **¥518.342tn** (exact).
- 20 Jul ~¥518.2tn → dashboard's end-July figure is ¥519.382tn; the doc's number is mid-month (20th), the dashboard's is end-month (31st) — a ~¥1.2tn drift over the back half of July is directionally sensible (JGS holdings were still gently declining), not a discrepancy once the different sampling dates are accounted for.

No numeric conflicts found between the doc and the fetched data — every spot-check ties out once you match the exact date.

### Not reached this session (Tier 1 remaining)

From the doc's Tier-1 list, these were **not** built this session — a future session should pick up here:
- **JGB ownership by investor type (stock)** — BOJ Flow of Funds (`boj.or.jp/en/statistics/sj/`) breakdown by BOJ/banks/insurers/pension funds/investment trusts/households/foreigners. This dashboard has strong JSDA *flow* data (who bought recently) but no *stock* picture (who owns the duration) — the doc calls this out as a real gap.
- **JGB liquidity / Bond Market Survey** — BOJ's public "Liquidity Indicators in the JGB Markets" + quarterly Bond Market Survey (`boj.or.jp/en/paym/bond/`).
- **Auction quality upgrade** — bid-to-cover, tail, rolling z-score, reopening-vs-new-issue for the existing Auctions & fiscal tab (currently just a calendar, no quality metrics).
- **Wages dashboard** — scheduled/total cash earnings, real wages, Shunto headline/base-pay increases (MHLW, JTUC-Rengo).
- **Fiscal issuance / duration-supply expansion** — gross/net issuance, issuance by tenor, average maturity, super-long share (MOF).

### Operational notes

- `fetch_boj_balance_sheet.py` is a **standalone script**, same pattern as every other `fetch_*.py` in this project — there is no shared daily orchestrator here to wire it into. It was **not** registered on any scheduled automation this session. BOJ Accounts data updates ~10th/20th/month-end (roughly every 10 days); **HanHan should decide** whether a monthly (or every-10-days) scheduled automation is worth creating to keep this fresh, given the tab is now live. Manual re-run: `python3.12 fetch_boj_balance_sheet.py`.
- Run fetch scripts with `python3.12` (per this workspace's standing convention — plain `python3` often lacks `requests`/`openpyxl` in this environment).
- Full regression swept via `agent-browser` after every change this session: every `<canvas>` on every tab confirmed to have a live `Chart.getChart()` instance (52+ charts total across the dashboard, zero without a chart), zero browser console errors, and every change confirmed live on the public URL (not just the local dev port) via direct `curl` of both `index.html` and the changed `.json` files.

## 2026-08-09 (later same day) — QT decomposition chart + 18-item balance-sheet upgrade doc received

HanHan sent a second, much more detailed feedback doc specifically on the BOJ Balance Sheet tab (18 numbered sections, priority-ranked ★★★★★ down to ★★) — a deeper elaboration of the same roadmap this file's "Not reached this session" list already sketched, now with a full analytical framework: MOF issuance → BOJ purchases/runoff → net private-sector duration supply → JSDA/Flow-of-Funds absorption → auction quality → JGB clearing yield/curve.

**Built same day: QT decomposition chart (doc's Section 2, ★★★★★, "most important upgrade")** — new `derived.jgb_redemptions_implied_jpy_tn` field added to `fetch_boj_balance_sheet.py`'s output: implied redemptions = gross outright purchases (DB=MD06) minus that month's net change in JGS holdings (DB=BS01), i.e. rearranging the doc's own identity (gross purchases − redemptions ≈ net change). **Not a new data pull** — both inputs already existed in the JSON, just hadn't been differenced. Values are directly plausible and self-consistent, not noise: implied redemptions spike to ~¥17-18.5tn every March/June/September/December (Japan's quarterly JGB maturity concentration) vs. ~¥1.3-1.9tn in other months — this is the real mechanism behind the "lumpy" net-change swings the existing `cBsJgsChg` chart already showed, and directly validates why the doc's Section 13 flags the 3-month-annualised contraction metric as misleading (a redemption-heavy quarter mechanically overstates the "taper pace"). New combo chart `cBsQtDecomp` (grouped bars: gross purchases +, implied redemptions −; line: net change) added to the tab right after the existing "Monthly gross JGB purchase operations" card, which already had a dangling reference to this exact comparison in its own note. Verified live via `agent-browser` (chart instance + pixel-check, 82k non-bg pixels, no console errors) and confirmed the new JSON field + regenerated chart are live on the public URL via `curl`.

**Not yet built — flagged as the next phase, in the doc's own priority order:**
- ★★★★★ Actual vs. BOJ-projected JGB holdings path (needs the BOJ's July 2024 taper-plan trajectory as a reference line — public info, moderate effort, no new data source).
- ★★★★★ Net JGB supply to private sector + who absorbs it (needs **MOF gross JGB issuance**, a genuinely new data source — this dashboard has never pulled MOF issuance amounts, only MOF cross-border BOP flows (`mof_flows.json`/`mof_weekly.json`, a different MOF series entirely) and JSDA super-long investor flows (`superlong_data.json`, reusable for the "who absorbs" half but only covers >10Y tenor, not all JGBs). **Investigated same day, not resolved**: confirmed the series exists — "公社債発行・償還および現存額（国内起債分）" (bond issuance/redemption/outstanding, domestic-issue basis) — and lives on the **same BOJ Time-Series API this project already uses** (`stat-search.boj.or.jp`), same family as the FM05 codes already in `fetch_boj_balance_sheet.py` (confirmed via `boj.or.jp/statistics/market/bond/ibgb_issue/index.htm`, whose own "search time-series data" link points at `lstSelection=FM05`). But the exact `SERIES_CODE` values for the issuance/redemption columns (vs. the already-known `SMBIT1OG`/`SMBIT1TF1` outstanding-amount codes) weren't found this pass — the API's `getDataCode` endpoint requires exact codes up front, there's no listing/search endpoint, and the BOJ's own code browser is a legacy Shift_JIS CGI form (`stat-search.boj.or.jp/ssi/cgi-bin/famecgi2`) that needs multi-step form navigation to scrape, not a quick lookup. Next step: either work through that legacy form to find the codes, or use the better lead found the same session — **MOF's own "国債等関係諸資料" page** (`mof.go.jp/jgbs/reference/appendix/index.htm`) directly links a historical **auction-results workbook**, `mof.go.jp/jgbs/reference/appendix/jgb_historical_data.xls` (479KB, "国債の入札結果" — every JGB auction's accepted/issued amount, presumably tenor + date, back many years). Summing accepted amounts per month across all tenors in that workbook would give a genuine actual monthly gross-issuance series with the same cadence as the existing MD06 purchases series — the same "aggregate from auction-level data" approach this workspace's Korea/BOK project already uses successfully for its own KTB issuance series (see `bok-hawk-dove-assessment/AGENTS.md`). Not yet downloaded/parsed this session — that's the concrete next step, not further BOJ API code hunting.
- ★★★★★ Purchases by maturity bucket (BOJ's daily operation-result HTML releases only, not a clean time series — same blocker `fetch_boj_balance_sheet.py`'s existing `operations.note` already flags).
- ★★★★★ BOJ absorption ratio (= BOJ purchases / MOF gross issuance) — blocked on the same missing MOF issuance series above.
- ★★★★ JGB ownership stocks by sector (BOJ Flow of Funds) — same gap this file already flagged pre-doc.
- ★★★★ QT vs. market functioning/liquidity (BOJ Bond Market Survey + Liquidity Indicators) — same gap already flagged.
- ★★★★ Auction quality (bid-to-cover, tail) upgrade to the existing Auctions & fiscal tab — same gap already flagged.
- ★★★ / ★★ Lower-priority items (relabel 3m-annualised as "volatile" or replace with cumulative windows, downgrade assets/GDP placement, current-account wording fix) — small, low-risk edits, not yet done, good candidates for a quick follow-up pass.
Bottom line: the single item buildable today with zero new data collection was the decomposition chart, and it was built and verified. Everything else genuinely needs one new official data source (MOF JGB gross issuance being the highest-leverage one — it unlocks 3 of the 6 top-priority items at once) before the rest of the doc's framework can be built safely.

## 2026-08-10 — MOF gross JGB issuance sourced; Net-supply + BOJ-absorption-ratio chart built

Picked up the concrete next step flagged above: found and parsed MOF's own auction-history workbook (`mof.go.jp/jgbs/reference/appendix/jgb_historical_data.xls`, one worksheet per tenor/instrument, columns located by Japanese header name since order/count vary slightly per sheet). New `fetch_mof_issuance.py` downloads it, sums each auction's Offering Amount (発行予定額 — spot-checked against the sum of accepted-competitive + non-competitive tranche columns on recent 10Y auctions, tracks to within ~0.1%, so used directly rather than reconstructing inconsistent per-sheet column layouts) by issue-month, across all coupon-bearing JGB tenors (2Y/4Y/5Y/6Y/10Y/10Y-linker/15Y-float/20Y/30Y/40Y/GX5Y/GX10Y). Treasury Discount Bills (sheet `'TB '`) are deliberately excluded — BOJ's own MD06 series tracks T-Bill outright purchases separately from JGB purchases, so the absorption-ratio comparison needs the same split. Output: `mof_issuance.json` (502 months, 1979-09 to 2026-06, ¥9-10tn/mo recent run-rate). Caught and fixed a real bug mid-build: first pass divided by 100 instead of 10,000 to convert 億円→兆円, which produced an absurd ¥1000tn/month total — corrected once the error was checked against a known-good single-auction figure. Also added a defensive trim: this workbook is a periodically-republished MOF snapshot (its internal save date trailed "today" by ~5 weeks at fetch time) and different tenor sheets stop at different auction dates within that lag window, so the script drops any trailing month whose total falls under half the prior-6-month average — a partial month, not a real dip (dropped July 2026 this run, leaving June 2026 as the latest complete month).

New chart, BOJ Balance Sheet tab, right after the QT-decomposition card: **`cBsNetSupply`** — "Net JGB supply to the private sector" (bars: MOF gross issuance − BOJ outright purchases, ¥tn/mo) with **BOJ absorption ratio** (line, right axis, % = BOJ purchases ÷ MOF gross issuance) overlaid. `index.html`'s balance-sheet-tab fetch changed from a single `fetch('./boj_balance_sheet.json')` to `Promise.all([boj_balance_sheet.json, mof_issuance.json])`, gracefully degrading (chart just doesn't render) if the MOF file is missing. Sanity-checked the computed series before shipping: absorption ratio runs 16-217% across the full 2012-2026 history (>100% during peak-QQE months is real and expected — MD06 purchases include secondary-market buying of already-outstanding bonds, not just that month's new issuance) and has fallen from ~40%+ in mid-2025 to ~25-27% by mid-2026 — the private-sector-absorption story the source doc was chasing, visible for the first time on this dashboard. Verified rendering live via `agent-browser` (canvas pixel-check, 48.8k non-bg pixels; Chart.js dataset dump cross-checked against the JSON; zero console errors) and confirmed `mof_issuance.json` is being served on the public URL via `curl`.

This unlocks 2 of the doc's 6 top-priority (★★★★★) items in one pass: "Net JGB supply to private sector + who absorbs it" (the "who absorbs" half — Flow-of-Funds ownership-by-sector — is still open, needs a different BOJ data source) and "BOJ absorption ratio". Not yet built from this same new `mof_issuance.json` source: a by-tenor issuance breakdown chart (the JSON already carries `by_tenor_jpy_tn` for all 13 tenors, unused on the dashboard so far) and "Actual vs. BOJ-projected JGB holdings path" (needs the BOJ's July 2024 taper-plan trajectory as a separate reference series, unrelated to this data source). `fetch_mof_issuance.py` is not on any automated schedule (matches this project's existing pattern — each `fetch_*.py` is run by hand) — it re-downloads the workbook fresh from MOF every run (unlike the emailed Korea REB property workbook, this MOF file lives at a stable public URL, so there's no reason to cache a stale local copy), so just re-run it whenever an updated read is wanted.

## July vs June 2026 Summary of Opinions comparison + speaker-attribution guesswork (2026-08-10)

HanHan asked for a comparison of the July 30-31 vs June 15-16 2026 BOJ Summary of Opinions, highlighting hawkish/dovish differences and read-through for the September MPM, to send to Angela. Built `soo-tone-tracker/july_vs_june_2026.md` (mirrored at workspace-root `BOJ_SOO_July_vs_June_2026.md`, the version actually emailed) — sourced both SOOs' official BOJ English translations directly (`opi260731.pdf` saved in this dir, `opi260616.pdf` fetched for comparison), built a 7-point checklist (downside-risk language, "deviate upward" phrasing, continue-hiking guidance, AI-demand narrative, dissent/nimble language, neutral-rate estimate, yen/FX language) plus full quote-level headline/prices/policy sections. Verdict: July is more hawkish underneath the hold, not a dovish pivot — the hold is framed as a transmission-lag pause while forward guidance, upside-risk-skew language, and "faster than market expectations" pace warnings all strengthened. Emailed to Angela (CC'd HanHan per her standing preference, see [[feedback_cc_hanhan_on_angela_emails]] in the Knowledge Vault memory).

HanHan then asked to go further: "guess which members say what." The SOO is officially anonymous — the Bank edits and publishes opinions without speaker names — so this is unverifiable inference, clearly caveated as such. Built `soo-tone-tracker/july_2026_speaker_attribution.md`: walked every Policy-Board-member bullet across all three SOO sections (Economic Developments, Prices, Monetary Policy — exact bullet-by-bullet breakdown is in the file; excludes the government-representative bullets from MOF/Cabinet Office, which aren't Board members) against three evidence sources already on this dashboard — each member's own 2026 speech quotes (`data.json → speakers`/`board`, e.g. Tamura's "should quicken the pace given stronger inflation risks" nearly word-for-word matches a July Monetary Policy bullet), the known 8-1 July vote (only Takata dissented, proposing 1.25%), and the SOO's own editorial convention of sequencing opinions from cautious to assertive within each subsection (cross-checked against this dashboard's own `spectrum` hawk-dove scores: Takata 2.5 > Tamura 2.0 > Koeda 1.5 > Ueda/Himino 1.0 > Masu 0.5 > Uchida 0.0 > Sato −0.5 > Asada −1.5). Headline finding: Takata's language ("nimbly," "discuss the size of a hike" rather than pace) maps High-confidence onto 3-4 of the most hawkish Monetary Policy bullets — consistent with a dissenter's full written rationale surviving the edit even though it lost 8-1 — but Tamura's own language (near-verbatim "quicken the pace," "double shock"/overshoot-avoidance framing) also maps onto 2-3 hawkish bullets despite him voting with the majority, a "wrote the hawkish case, didn't dissent" pattern worth watching for a possible Tamura dissent if September data firms up. Not yet sent to Angela — HanHan didn't ask for that this time, only the comparison+attribution itself; offered to email it in the Telegram reply.

## 2026-08-10 (later same day) — Balance Sheet tab review doc actioned, all 5 priorities

HanHan forwarded a detailed expert review of the net-supply/absorption-ratio chart shipped earlier the same day, prioritized 1-5. Actioned all five in one pass:

**1. Fixed the "net JGB supply" mislabel** — the existing chart (MOF gross issuance − BOJ purchases) was relabeled "Gross JGB issuance less BOJ purchases — private-sector absorption requirement" (the doc's own suggested exact wording), with the note rewritten to state plainly it's gross, not net of redemptions on either side.

**2. Sourced MOF's real new-money-vs-refunding split** — found MOF's own "JGB IR Basic Materials" investor presentation (2 Mar 2026, `mof.go.jp/insideLink/20260302190754.html`) and the FY2026 Issuance Plan page (`mof.go.jp/jgbs/issuance_plan/fy2026/`), which break FY2026 (令和8年度) planned issuance out by legal basis: ¥180.7tn total = ¥135.8tn refunding bonds (75%, replaces maturing debt, not new duration) + ¥29.6tn new-money financing bonds (16%) + ¥13.0tn FILP bonds + ¥2.3tn GX transition bonds — so true new net duration to the market ≈ ¥44.9tn/yr (~¥3.7tn/mo), a very different number from the ¥180.7tn gross figure the old chart implicitly compared BOJ purchases against. New KPI card "FY2026 MOF issuance plan — new-money vs. refunding" added right below the gross chart. Caveat disclosed in the card: a June 2026 supplementary revision raised the total to ~¥183.8tn but a clean re-split by legal basis wasn't found from a single clean source in the time available, so the shares shown are the original-plan basis.

**3. Free-float / liquidity indicator** — new chart "Free-float JGB supply — BOJ holdings vs. private-sector holdings," computed directly from data already in `boj_balance_sheet.json` (`derived.jgb_outstanding_total_jpy_tn` minus `assets.jgs`, zero new data pull). This reframes the existing BOJ-ownership-share chart's same two numbers as what's actually tradable — genuinely interesting output: private-sector JGB holdings bottomed around 2023 and have been climbing since QT started, i.e. QT is a real liquidity tailwind even as it's a duration headwind, exactly the two-sided story the review doc wanted surfaced.

**4. Non-JGB section** — new chart + KPI row for ETFs/corporate bonds/J-REITs, using `assets.etf`/`assets.corporate_bonds`/`assets.jreit` (already-collected monthly series, previously only shown in the latest-month composition donut, never as a time series). Computed trailing-6-month pace and a naive linear years-to-unwind per series, clearly caveated as a pace indicator not a BOJ-announced schedule. Real finding: corporate bonds are unwinding fast (~1 year to zero at current pace, consistent with the BOJ's 2022 no-reinvestment policy), while ETFs and J-REITs are barely moving (~108 years at current pace) — direct, dashboard-native evidence for the doc's own point that ETF disposal "has become more politically relevant."

**5. Market-impact panel** — new 3-chart card joining BOJ absorption ratio against (a) 10Y/30Y month-end yields from the already-existing `market_data.json → jgb.history`, (b) a brand-new 30Y auction tail (bp) + bid-to-cover series built from the same MOF auction-history workbook already downloaded for the issuance data (`fetch_auction_quality.py`, new `auction_quality.json` — tail = yield-at-lowest-accepted-price minus yield-at-average-price, ×100 for bp; also covers 10Y as a bonus), and (c) JSDA's existing super-long (>10Y) net-buying-by-investor-type monthly panel (`superlong_data.json → players.superlong`, already collected for a different tab, reused here — initially miswired to the wrong key (`series`, which is FY-bucketed annual data) instead of `players.superlong` (the actual flat monthly panel), caught and fixed via a blank-chart check in `agent-browser` before shipping). Explicitly labeled a "first-pass correlation view, not a causal test" per the doc's own framing.

**Also addressed in passing:** doc's item 5 ("de-emphasise 3m, highlight 6m/12m") — the existing contraction-pace chart's 3m line was changed to a thin dashed grey line and 6m/12m bolded, relabeled "(distorted by redemption spikes)" / "(preferred)" directly in the legend.

**Partially addressed, flagged as still open:** doc's item 2 (maturity-bucket + duration/DV01-weighted absorption) — built the issuance side only ("MOF gross issuance by maturity bucket," 5 buckets from 1-3Y to 25Y+, using `mof_issuance.json`'s already-collected `by_tenor_jpy_tn`, windowed to the last 7 years for readability against the full 500-month history). The BOJ-purchases-by-maturity-bucket half remains genuinely blocked — same gap already documented earlier in this file ("only in BOJ's daily operation-result HTML releases, not a clean scrapable time series") — so a true duration-weighted absorption measure isn't buildable without that data source. Flagged explicitly in the new card's own subtitle ("issuance side only — see note"), not silently left half-finished.

All 7 new/changed elements verified live via `agent-browser` (pixel-checked every new canvas, confirmed non-zero content; checked console for errors after each addition, none found) and cross-confirmed via `curl` against the public URL. New files: `fetch_auction_quality.py` (+ `auction_quality.json`) — not on any automated schedule, matches the project's `fetch_mof_issuance.py` pattern (re-downloads/re-reads the already-cached workbook each run).

## Institutional-strategist review, 2026-08-10 — top-level stale-data badge shipped, rest scoped

HanHan forwarded a detailed Japan Macro/BOJ dashboard critique (institutional rates-desk framing: hawk/dove scoring mechanics, curve/swap gaps, data-lag alignment, UI workflow, plus 5 categories of proposed public-data additions). Did a feasibility/duplication check against this project's existing build before touching anything.

**Shipped — top-level stale/missing-data badge** (review's item C1: "expand the Data source status table into top-level visual badges"). New `#staleBadgeTop` element under the header `asof` line, populated inside `renderMarkets(M)` right after the existing `sourceStatus` table build — counts non-`ok` entries in `M.sources`, shows "⚠ Data feeds: N missing, M stale — see Data source status ↓" in amber (stale) or red (missing, using the existing `--hawk2`/`--accent` CSS vars, no new colors invented), hidden entirely when everything's healthy. Clicking it jumps to the Dashboard tab where the full table lives. Verified live: badge correctly hidden (all 8 tracked sources currently `ok`); synthetic test via `agent-browser eval` (temporarily calling `renderMarkets` with a fake stale+missing `sources` object) confirmed it renders correctly with the right count and color, then reloaded to restore real state. Zero console errors.

**Caveat worth flagging separately:** the `sourceStatus`/`src` object this badge reads only tracks ~8 sources (`jgb`, `fx`, `nikkei`, `us10y`, `call_rate`, `inflation`, `potential_growth`, `news`) — it predates most of this project's later fetch scripts (`fetch_boj_balance_sheet.py`, `fetch_mof_bop.py`, `fetch_mof_flows.py`, `fetch_mof_issuance.py`, `fetch_mof_weekly.py`, `fetch_natural_rate.py`, `fetch_auction_quality.py`, `fetch_jsda_superlong.py`, `fetch_intervention.py`), none of which report into it. So the new badge (and the underlying table it summarizes) genuinely can't catch a stale/failed run in most of this dashboard's data — a real gap, separate from what was asked, not fixed this pass since it'd mean wiring status-reporting into ~9 more fetch scripts. Worth a dedicated follow-up if HanHan wants full coverage.

**Not built, everything else — flagged back to HanHan rather than guessed at**, since this review is comparable in scope to the earlier BOK one:
- Confirmed already covered/already-investigated, not fresh gaps: OIS/swap proxy was explicitly considered and excluded already (seasonality tab's own note: "no reliable free swap-curve data source"). BOJ ownership share exists as an aggregate chart but not broken out by maturity bucket — same already-documented blocker as the DV01-weighted absorption gap above (BOJ purchases-by-maturity only exist in daily operation-result HTML, not a scrapable time series). Distribution-based inflation measures (trimmed mean/median/mode) already exist but are a different thing from household/corporate 1Y/5Y inflation-expectations survey data (BOJ Opinion Survey) — that survey itself is not yet pulled.
- Genuine data-discovery gaps, not yet checked for free-source feasibility: JGB futures (JPX/Osaka Exchange settlement/volume/OI), USD/JPY cross-currency basis or an FX-hedged yield differential proxy, Shunto wage negotiation data (Rengo), rinban actual-vs-scheduled monthly pacing specifically (vs. the gross-pace chart that already exists).
- Judgment call needing sign-off, not built without it: hawk/dove scoring rubric + board-member weighting (Ueda/Uchida weighted higher than back-bench members) — same category as BOK's composite-reweighting ask, since it would change how the flagship spectrum score is computed, not a quick patch.
- CSV/PDF export for auction calendar / seasonality matrices — genuinely missing (checked, zero `exportBtn`-style pattern anywhere on this dashboard, unlike the AI dashboard which already has this pattern established) — a real, low-risk quick win, just not built this pass to keep the response focused; flagged for a future round if wanted.
- Event-driven seasonality windows (FYE, new fiscal year, Oct 1, pre-auction concession) — likely buildable from already-fetched daily JGB yield data (`build_seasonality_data.py`'s existing calendar-month backtest infrastructure), not yet scoped in detail.

## Outbound sovereign-bond-by-country card fixed to match Nomura/Mizuho/SMBC — real fix, root cause found (2026-08-10)

HanHan flagged (via a Nomura screenshot replica request, then a Mizuho screenshot, then an SMBC Nikko screenshot) that the "Japan, outward portfolio investment, sovereign bonds, long-term, net — by country" card's numbers didn't match sell-side research, worst on US Jan-2026 (our ¥293.2bn vs Mizuho's ¥444bn, a 34% gap) despite ~95 other checked cells matching within rounding. Root cause fully diagnosed, not just caveated:

**MoF's preliminary BOP press-release PDF (`bppiYYYYMM.pdf`, what `fetch_mof_bop.py` parsed) is a first-release snapshot that MoF/BOJ revise afterward without ever updating the static PDF in place.** Confirmed by direct evidence, not inference: our parser reads the PDF correctly (raw PDF text literally contains "2,932" = ¥293.2bn for USA, matching our output exactly — ruled out a parsing bug); ruled out an off-by-one-month file mismatch (each bppiYYYYMM.pdf's own internal title/summary-table row confirms it covers the month its filename says); ruled out a rolling-window column-selection bug (the country table appears exactly once per PDF); confirmed via fresh `curl` + `md5sum` that MoF's PDF URLs are static archives, never silently updated. MoF's own `bppi.htm` page explicitly states this exact table ("主要国・地域ソブリン債への対外証券投資") is NOT published anywhere on MoF's site as a static/CSV file — only via **BOJ's Time-Series Data Search site** (stat-search.boj.or.jp), which IS continuously revised. This is almost certainly what Nomura/Mizuho/SMBC actually pull (SMBC's own screenshot cited "Bank of Japan, SMBC NIKKO" for this specific table, distinct from "Ministry of Finance, SMBC NIKKO" on its other charts — the clue that cracked this).

**Found and pulled the real series.** stat-search.boj.or.jp is a legacy 1990s-era FAME CGI system (`/ssi/cgi-bin/famecgi2`) behind Akamai bot protection — plain `curl`/`requests` POSTs get flat-out rejected (400/411 from `errors.edgesuite.net`), and even Zo's hosted browser tool (`open_webpage`/`use_webpage`) timed out twice trying to interact with it. What worked: the local `agent-browser` CLI, driving the real page DOM directly via `agent-browser eval` (the on-page checkbox/button click handlers are wired through delegated jQuery listeners, so `agent-browser check <ref>` alone doesn't fire them — had to call the page's own JS functions directly: click the checkbox via `.click()` to fire real events, `addAbstractCondition('#dataSeriesSearchDataCodeList')` to add to the extraction list — note there are 3 near-identical `addAbstractCondition(...)` buttons, one per search tab (menu/keyword/data-code); grabbing the wrong one silently no-ops — then `submit_code_main(document.nme_A000_form)` to extract). Keyword search "ソブリン" under category BP01 surfaces the exact series codes: `BP01'BPPI6D1N{XX}` = "対外証券投資 ソブリン債/合計/{country}/ネット" (Total sovereign bonds, country, Net) for XX ∈ {US,CA,AU,DE,FR,IT,NL,GB,DK,CH,HK,SE} plus `AOT` (その他/Others) — exactly matches this project's existing 12-country + others structure, no remapping needed. Unit confirmed empirically as 億円 (raw value ÷10 → ¥bn): Jan-2026 US came back as 4505.975… → ¥450.6bn, just 1.5% off Mizuho's ¥444bn (vs. the PDF's 34% miss) — decisively confirms this is the right source and the right unit.

**Fix applied:** pulled all 13 series (12 countries + others) for 2021-01 through 2026-06 via the above method, rebuilt `mof_bop.json`'s `outbound_sov.series`/`series_usd` from this BOJ data (USD leg recomputed with the existing monthly-USDJPY conversion), `total` recomputed as the sum of the 13 (no separate BOJ "total" series was pulled — sum-of-parts is the best available total). `inbound` (foreigners buying JGBs by country) was untouched — it was never the source of the discrepancy, still sourced from the MoF PDF as before.

**Made this durable against the monthly automation clobbering it back:** `fetch_mof_bop.py` no longer regenerates `outbound_sov` from the PDF at all — it now loads whatever's already in `mof_bop.json` and preserves it untouched, only refreshing `inbound`. The "Monthly MoF Cross-Border Flows Data Refresh" automation (runs ~10th of each month) instructions were updated to match: it still runs `fetch_mof_bop.py` for the `inbound` refresh, but its Step 5 Telegram report no longer claims an "outbound-sovereign total" update, and it's told to flag (not silently patch) if `outbound_sov` starts looking stale.

**This means `outbound_sov` is now a manually-refreshed series, not an automated one** — there's no headless path to BOJ's site (Akamai blocks it), so extending it past 2026-06 requires repeating the `agent-browser` method above in an interactive session: open `http://www.stat-search.boj.or.jp/ssi/cgi-bin/famecgi2?cgi=$nme_a000&lstSelection=BP01`, switch to the 系列名称検索 (keyword search) tab, search "ソブリン", `.click()` the 13 `BP01'BPPI6D1N{XX}` checkboxes (Net only — ignore the 取得/Acquisition and 処分/Disposition variants, and the `（更新停止）`-prefixed discontinued-series duplicates), call `addAbstractCondition('#dataSeriesSearchDataCodeList')`, set the from/to year fields, call `submit_code_main(document.nme_A000_form)`, then click "時系列データ表示（月次）" on the extraction-options tab that opens — the resulting page is a plain tab-separated table, one column per series in the order selected. Re-run the same JSON-rebuild math (values ÷10 → ¥bn, USD leg via existing FX helper) and overwrite `mof_bop.json` (both root + `streamlit_app/` copies) directly — do not re-enable `fetch_mof_bop.py`'s old PDF-based `outbound_sov` path.

Reported to HanHan with the concrete before/after (¥293.2bn → ¥450.6bn vs Mizuho's ¥444bn) rather than just "fixed it" — this was a genuine root-cause fix, not a caveat/footnote, per her standing preference for transparent, verified handling of data discrepancies.

## BOJ calendar was missing all board-member speeches + fixed a stray-file bug, plus added the Bessent/Ueda meeting (2026-08-10)

HanHan asked "how come the speeches from Japan members are not included in boj calendar." Root cause: **`index.html`'s `loadData()` fetches `./streamlit_app/data.json`, but a prior session's speech additions (Himino, Takata) were written to the root-level `/home/workspace/boj-hawk-dove-assessment/data.json` instead** — a stray, unread duplicate (confirmed via grep: nothing in `index.html` or any `.py` script here reads the root file; `collect_scrape.py`/`validate_data.py`/every `fetch(...)` call in `index.html` all target `streamlit_app/data.json`). So the two speeches existed in a file, just not the one the live site serves — `streamlit_app/data.json`'s `mpm_calendar` had zero `Speech`-type entries. **If editing `mpm_calendar` (or likely anything else) in this project again, always edit `streamlit_app/data.json` — do not trust or edit the root `data.json`,** which appears to be dead weight left over from an earlier architecture (not deleted this pass, in case something undiscovered still depends on it, but flagged here as suspect).

Fixed by adding 4 real entries to `streamlit_app/data.json → mpm_calendar` (chronological, between Aug 10 SOO and the Sep 17–18 MPM): the 3 speeches BOJ has actually scheduled per its own official "Scheduled Dates of Upcoming Speeches" table (`boj.or.jp/en/about/press`, checked live 2026-08-10) — Aug 27 HIMINO Ryozo (Saitama), Sep 2 TAKATA Hajime (Sapporo), and **Sep 10 MASU Kazuyuki (Fukui) — this third one had never been added at all**, not even to the stray root file — plus a new **"G20 Finance Ministers & Central Bank Governors — Bessent/Ueda expected meeting"** entry (Aug 31–Sep 1, Asheville NC) at HanHan's follow-up ask, sourced from Bessent's own public remarks + Reuters/Japan Today (Aug 1–5, 2026): Bessent has said he expects to meet Governor Ueda there, and Reuters sources say that meeting (if it happens) would make a September hike hard to avoid — flagged in the entry's own notes as *expected/reported, not an officially BOJ-confirmed event* (BOJ has denied at least one earlier reported Bessent-Ueda meeting, May 13, 2026).

Added a new `Meeting` calendar-type/chip (`.c-meet`, hawk-red tint, same visual weight as the existing MPM/Speech/Minutes chips) to `index.html` for the G20 entry — `chipHtml()`'s type→class map and the `.c-meet` CSS rule are new; no other calendar-rendering logic touched. Verified live via `agent-browser` on `localhost:8501`: all 4 new rows render in the correct chronological position with correct chip styling, `mpm_calendar` now has 28 entries (was 24), zero console errors. Pre-existing, unrelated `validate_data.py` warnings ("spectrum/tone score out of range for Takata") were already present before this edit — not caused by it, not fixed this pass (out of scope; a hawk-dove scoring-rubric question, not a calendar one).

**Notes shortened same day** — HanHan flagged the "Reuters/Japan Today, Aug 1–5, 2026" citation as reading confusingly (the outlet name "Japan Today" sitting right next to a date range made it look like it was claiming something about "today" spanning multiple days) and asked to shorten all 4 new entries' notes. Rewrote all 4 (Himino, G20/Bessent-Ueda, Takata, Masu) down to 1-2 short sentences each — same facts, no new research, just trimmed — and fixed the ambiguous phrasing to "Japan Today, early Aug 2026" (clearly source + approximate date, not a claim about "today"). Verified live via `agent-browser`, zero console errors.

**Past calendar rows dimmed, same day** — HanHan: "can you make past events less obvious (like what you did to old mpm members who already left board)" — referencing the Board & votes tab's existing `tr.gone td{opacity:.55}` treatment for departed members. Added a `calEndDate(e)` parser to `render()`'s calendar block that reads the end-date out of each row's free-text `date` string (handles same-month ranges like "Jul 30–31", cross-month ranges like "Aug 31 – Sep 1", single dates, the "~Dec 14" prefix, and the `_yr` field for 2027 rows), compares it against today (midnight-truncated `new Date()`), and adds the existing `gone` class to any row whose end date is strictly before today — reusing the board tab's CSS rather than inventing a new one. Today's own events are NOT dimmed (only strictly-past rows), so e.g. "Aug 10 — Summary of Opinions" stays fully visible on Aug 10. This is computed against the visitor's real wall-clock date every page load, not a static flag, so rows dim automatically as time passes — no manual upkeep needed. Verified live via `agent-browser`: as of 2026-08-10, exactly the 3 rows before today (Jul 30–31 MPM, Jul 31 Outlook, Aug 5 Minutes) render dimmed; everything Aug 10 onward (including the 2027 section) renders at full opacity; zero console errors.

## Auctions & fiscal tab: same past-event dimming + September 2026 auction calendar added (2026-08-10)

HanHan, same day, extended the ask: "for auction calendar also, for past events, can you use the same effects. also can you add september auction calendar." Both done in `streamlit_app/data.json → auctions` + `index.html`'s Auctions render block (separate from the MPM `mpm_calendar` block above — this tab has its own month-keyed `auctions` object and its own render loop, so the dimming logic had to be added a second time, not shared automatically).

**Dimming:** each auction row's `date` field (e.g. "Jul 2 (Thu)") already includes the month, so a lighter-weight version of the same `CAL_MON`-based parser was added directly to the Auctions render loop — the section heading itself ("July 2026", "August 2026", "September 2026") supplies the year via a quick regex, then any row whose parsed date is before today's midnight gets the same `gone` class as the calendar/board tabs. Reused `calToday` (already computed earlier in the same `render(D)` function for the MPM calendar) rather than recomputing "today" a second time.

**September added, sourced directly from MOF's own auction calendar** (`mof.go.jp/jgbs/auction/calendar/2609.htm` — found via that site's `YYMM.htm` URL pattern, e.g. `2604.htm`=April, `2608.htm`=August), not estimated from a monthly-cadence pattern: 14 auctions — 10Y (Sep 1), 30Y (Sep 3), T-bill 3mo (Sep 4), 5Y (Sep 8), T-bill 6mo (Sep 9), Liquidity Enhancement 1–5y (Sep 10), T-bill 3mo (Sep 11), 20Y (Sep 15), T-bill 1y (Sep 16), T-bill 3mo (Sep 17), T-bill 3mo + Liquidity Enhancement 5–11y (both Sep 25), 40Y (Sep 29), 2Y (Sep 30). All weekday labels cross-checked against a real calendar computation, all matched MOF's own labels. No 10Y linker or GX bond this month — MOF's September page genuinely doesn't list either (unlike August, which had both), so none were added/guessed. Context notes added sparingly, same house style as July/August (e.g. Sep 16 T-bill "Day before the Sep 17–18 MPM decision", Sep 17 T-bill "MPM day 1 (Sep 17–18)", Sep 29 40Y "first since the Jul 22 40y auction") — most rows left with empty context, matching the existing months' pattern of only annotating auctions with a real read-through.

Verified live via `agent-browser`: July (all 15 rows) and the first 4 August rows (Aug 4/6/6/7) render dimmed; Aug 12 onward plus all of September render at full brightness; September section renders with all 14 rows and correct context text; zero console errors.

**Month sections made collapsible, same day** — HanHan: "for july fiscal, is it possible to do a click to minimise it? coz it takes up a lot of space if we have to show past auctions." Each month's `<h3>` header in the Auctions render loop is now clickable (`toggleAucMonth(header, sid)`, mirrors the existing `toggleBoardRemarks` chevron-toggle pattern on the Board & votes tab) and collapses/expands that month's table via a wrapper `<div id="auc-sec-N">`. **Auto-collapsed by default only when every row in that month is past** (reuses the same per-row `isPast` check as the dimming logic, via `.every()`) — so July (fully past) loads collapsed with a "— auctions completed, click to expand" hint next to the heading, while August (partially past) and September (all future) load expanded. This means the behavior adapts automatically going forward: August will auto-collapse on its own once every one of its rows is in the past, no manual re-edit needed. Clicking any header re-expands/re-collapses it, chevron flips ▶/▼ same as the board tab. Verified live via `agent-browser`: July loads collapsed, click expands it (chevron ▼, table reappears with dimmed rows intact), zero console errors.

## BOJ Balance Sheet tab hidden here, ported to Japan NISA site for review (2026-08-10)

HanHan asked to move the "BOJ Balance Sheet" tab to the `japan-nisa-tracker` dashboard so she can review it carefully before it's shown anywhere, and explicitly asked to not show it on this (BOJ) site in the meantime.

**Hidden, not deleted:** the tab button got `style="display:none" data-hidden="pending-nisa-review-2026-08-10"` (same pattern as this file's existing hidden-tab convention, e.g. the NISA tab button's own `data-hidden="redesign"`). The panel div (`id="tab-balancesheet"`) and all its underlying fetch scripts (`fetch_boj_balance_sheet.py`, plus its dependencies on `mof_issuance.json`/`market_data.json`/`superlong_data.json`/`auction_quality.json`) were **not touched** — still run on their normal schedule. To unhide: find `data-hidden="pending-nisa-review-2026-08-10"` in `index.html` (1 hit, the tab button) and remove `;display:none` + the attribute.

**Ported (copied, not moved) to `japan-nisa-tracker/index.html`** as a new draft tab, labeled "draft — review only" — full details in `japan-nisa-tracker/AGENTS.md`. That copy reads one-time snapshots of the 5 JSON files this tab needs (`boj_balance_sheet.json` + 4 dependencies), not a live automation — it will go stale if not re-copied before review. Verified both sides live via `agent-browser`: this site's tab bar no longer shows Balance Sheet (rest of the dashboard unaffected), and the NISA site's new tab renders all ~14 charts correctly.

## Balance of payments overview: NSA + USD + horizon toggles, moved to end of tab side-by-side (2026-08-11)

HanHan, after the Nomura-style axis cleanup (above): "For current account, possible to give me different horizon toggle, USD conversion also? Also possible to have NSA version?" then, separately, "I want to move the ca and outbound investment to last charts - make it side by side."

**NSA source found by reading `bpnet.htm`'s own CSV list, not guessed:** MoF's SA current-account file (`bpnet/sbp/s-a/6s-a-2.csv`, already used) sits in a `s-a` (季節調整済) family; the un-adjusted (原数値) monthly equivalent is `bpnet/sbp/s-1/6s-1-4.csv` — same table ("国際収支総括表"), same column layout, labeled "Not seasonally adjusted" in its own header row. `fetch_mof_bop_summary.py`'s `fetch_current_account()` now takes a URL param and is called twice (SA + NSA); output gained `current_account_nsa` (same shape as `current_account`).

**USD conversion** reuses `fetch_mof_flows.py`'s `usdjpy_monthly()` pattern (Yahoo `JPY=X`, monthly close) but with `range=max` instead of `7y` since this series needs the full 2015-present window. Every ¥tn series (current_account, current_account_nsa, direct_investment's 4 fields) now has a `_usd` sibling computed server-side ($bn = ¥tn × 1000 / that month's rate, nearest-earlier-month fallback if a month is missing) — same convention as the existing `monthly_usd` fields in `mof_flows.json`, so the frontend just picks which pre-computed field to plot rather than dividing in JS.

**Frontend:** the "Balance of payments — overview" section (KPIs + the two charts) moved from the top of the Cross-border flows tab to the very end (after "Weekly cross-border flows"), and the two chart cards are now a 2-col grid instead of stacked full-width. New control row above the charts: 1Y/2Y/3Y/5Y/All window (`setBopWin`), ¥tn/$bn currency (`setBopCur`, applies to both charts), and Current account SA/NSA (`setBopAdj`, current-account chart only — direct investment has no SA version at the source, so no toggle needed there). State lives in `_bopSumData`/`_bopWin`/`_bopCur`/`_bopAdj` — **deliberately renamed from the first draft's `_bopData`**, which collided with the pre-existing `_bopData` used by the by-country inbound/outbound charts (`renderBop()` above it) and silently broke the whole page (`Identifier '_bopData' has already been declared'` — a `<script>`-wide `SyntaxError`, so the entire tab bar stopped switching, not just this section). Caught by parsing the page's `<script>` block with Node before reloading the browser — worth doing that check first next time a duplicate-looking variable name is introduced, it's faster than a live reload+screenshot round-trip.

KPI row now respects both the currency and SA/NSA toggles (shows "$0.6bn (NSA)" etc. when selected) — the four figures are always the *latest* month regardless of the window toggle, same as before.

`fetch_mof_bop_summary.py` is called from the existing "Monthly MoF Cross-Border Flows Data Refresh" automation (STEP 2.5) and fully regenerates `mof_bop_summary.json` from scratch every run (no preserved blocks) — the new NSA/USD fields need no automation changes, they'll just appear on the next scheduled run.

Verified live via `agent-browser`: default view (SA, ¥tn, All) renders correctly at the bottom of the tab, side by side; toggling NSA + $bn + 1Y together updates KPIs and both chart axes/data correctly (checked via `document.querySelector('#bopWinBtns button.active')` and a screenshot showing $bn-scaled bars over a ~12-month window); zero console errors.

## Outbound sovereign-bond card: real label/data bug found via Macrobond, fixed with LT-only/LT+ST toggle + size filter (2026-08-11)

Supersedes the "do NOT re-enable `fetch_mof_bop.py`'s old PDF-based `outbound_sov` path" instruction from the 2026-08-10 section above — that instruction is now stale, see below.

HanHan: "I can see from macrobond the latest June data for US is 883bn outflow, but it's not shown from our table." Traced it to the exact MoF appendix table (`bppi202606.pdf`, "Portfolio Investment Assets, Country Breakdown of Sovereign Bonds") and found the table has **two separate US net columns**: Sovereign (Long-term only) = -¥883.0bn (exact match to Macrobond) and Sovereign (Total, LT+ST) = -¥967.4bn (what the dashboard was actually showing). The card was labeled "long-term" but the 2026-08-10 manual-BOJ-pull freeze (see section above) was actually sourcing the **Total** metric, not LT-only — confirmed by checking Feb-Jun 2026: the frozen values track the PDF's Total column almost exactly (e.g. Jun US -967.36 frozen vs -967.4 PDF-Total), never the LT-only column (-883.0). A real label/data mismatch, not a one-off discrepancy.

**Fixed properly, not just relabeled**, since HanHan's immediate follow-up ask was a toggle between both bases plus a size filter — building that required a single coherent, auto-refreshing source for both metrics. `fetch_mof_bop.py`'s `extract_sovereign` was rewritten to `extract_sovereign_multi(lines, countries)`, which parses BOTH the Sovereign(Total) net (col idx 2) and Sovereign(Long-term) net (col idx 5) from the same table row in one pass (9-number row layout: `[0]TotalAcq [1]TotalDisp [2]TotalNet [3]LTAcq [4]LTDisp [5]LTNet [6]STAcq [7]STDisp [8]STNet`). `main()` now builds `outbound_sov` (= LT-only, matches the card's existing label and Macrobond) and a new `outbound_sov_total` (= LT+ST) block, both **fully auto-regenerated from the PDF every run** — the 2026-08-10 freeze-and-preserve logic was removed entirely, `mof_bop.json` is no longer partially frozen. Re-verified across all 50 cached months (2021-11 to 2026-06) that this doesn't reintroduce the original 34%-gap problem: PDF-Total tracks the old frozen-BOJ values closely for every month except the same one outlier already known (Jan-2026, ~30-35% gap vs Mizuho) — that risk is now stated directly in the card's own footnote rather than solved by manual BOJ pulls (which have no headless path anyway, see the Akamai-blocking detail in the 2026-08-10 section above — this was also a maintenance-burden problem, not just an accuracy one).

**UI**: `index.html`'s "Where Japan buys foreign sovereign bonds" card gained a basis toggle (`Long-term only` / `Long-term + short-term` buttons, `setBopOutBasis()`) and a "Show: All countries / Top 5 by size / Top 8 by size" dropdown (`setBopOutTopN()`, ranks the 12 tracked countries + Others by \|latest month's value\|). Both controls drive `renderBop()`, which now computes a filtered `order` array (`bopOutOrder()`) shared by the stacked-bar chart (`bopChart()`, now takes an `orderOverride` param) and the table below it (`bopOutTable()`, now takes `sec`/`orderOverride` params instead of hardcoding `outbound_sov`). The white "whole-market total" line in the chart is deliberately **not** affected by the Top-N filter — it always sums all 13 tracked buckets (12 countries + Others), so filtering to Top 5 hides bars but the context line stays the true total; stated explicitly in the card's sub-text so a user doesn't assume the bars should sum to the line.

**Automation updated to match**: the "Monthly MoF Cross-Border Flows Data Refresh" automation (id `16d37a68…`, runs ~10th of each month) had its instructions rewritten via `edit_automation` — the old Step 2 text explicitly told future runs "do NOT re-enable the PDF-based outbound_sov path"; that's now replaced with instructions describing the dual-metric auto-regeneration and asking Step 5's Telegram report to include both LT-only and Total US figures, plus a new watch-for: if outbound-sovereign figures stop changing month-over-month for 2+ runs while other feeds advance, flag it (possible silent PDF-table-structure-change parse failure) rather than assuming non-publication.

Verified live via `agent-browser` on the public URL: both toggle states render correctly (LT-only vs Total swaps the bar heights and legend order but not the white total line), Top-5 filter correctly narrows the legend/table to 5 rows ranked by June's magnitude (US, Others, Australia, France, UK that month) while the white line/Total row stay full-market, zero console errors. Committed (`a393dfe`).

## Weekly cross-border flow charts: sign-tinted fill + 3M rolling-average toggle (2026-08-11)

Two quick follow-up asks on the "Both directions, weekly" section (the two side-by-side line charts, `cXbWeeklyOut`/`cXbWeeklyIn`, powered by `mof_weekly.json`).

**Sign-based color:** each line's own stroke color still distinguishes Equities (red) vs Long-term debt (grey) — that's load-bearing for the legend, so it wasn't touched. Added a Chart.js `segment.backgroundColor` scriptable fill instead: the area under each line now tints green (`rgba(76,186,122,0.18)`) when the segment's value is ≥0 and red (`rgba(226,86,74,0.18)`) when negative — matches this dashboard's existing green/red up/down convention (`#4cba7a`/`#e2564a`, same as `.d.up`/`.gpif-up`/`.status-ok` elsewhere). Both lines keep their own fill target at `origin` so the two areas can overlap without one obscuring the other's stroke.

**3M rolling average toggle:** new "Weekly" / "3M rolling avg" button pair next to the existing 6M/1Y/2Y/3Y/All window buttons, state in `_xbWeeklySmooth`. `xbwRollingMean(arr, 13)` computes a strict trailing 13-week mean (13 weeks ≈ 3 months) — **null until 13 real data points are available, no partial-window averaging**, consistent with this project's standing convention of never fabricating values for periods without enough history. Computed on the *full* series before the window slice (`i0`), not after, so the earliest visible points in a 6M/1Y view still have their full 13-week trailing history rather than being null for the first ~3 months of whatever window is selected.

**Bug caught and fixed in the same pass:** the y-axis tick callback (`callback:v=>(v>0?'+':'')+v`) directly string-concatenated Chart.js's auto-computed tick values, which — once the rolling average shrank the axis range to small decimals (e.g. -0.6 to +0.4) — exposed classic JS float-accumulation artifacts in Chart.js's own internally-generated tick steps (`-0.4000000000000001` etc., not from the underlying data). This was latent in the original raw-weekly view too (never triggered since that range's ticks land on cleaner numbers) — fixed by rounding the tick value itself (`Math.round(v*100)/100`) in the callback, scoped only to this chart's y-axis (7 other charts share the same unrounded pattern elsewhere in `index.html` and were left untouched — not in scope for this ask, but worth the same fix if anyone hits it there).

Verified live via `agent-browser`: sign-tinted fills render correctly under both lines in both panels at 6M zoom (clearly visible green/red split); 3M toggle updates legend labels to "(3M avg)", chart status text to "· 3M rolling avg", and axis now shows clean ticks (-0.6 … +0.4, no float artifacts); toggled back to Weekly/All confirmed original view unaffected; zero console errors. Committed (`c80ac5e`).

## NISA tab unhidden + Investment Trusts flow card added (2026-08-11)

HanHan sent a Macrobond screenshot ("Portfolio Investment Flows by Investment Trust Management Companies") and asked whether a NISA tab should be added to this dashboard. Investigation found a full NISA tab already built here (FSA whole-market headline, JSDA 10-firm monthly pulse, quota split, allocation, target progress) but hidden (`data-hidden="redesign"`) with data stuck at Mar-2026 — same root cause as `japan-nisa-tracker`'s staleness (both read/mirror the same `nisa_data.json`, see that project's own AGENTS.md entry same day for the fetch-script fix). Also found HanHan's screenshotted series already sitting unused in `mof_flows.json` (`monthly.invtrust` / `monthly_by_instrument.{equity,ltbonds,stbonds}.invtrust`) — the MoF weekly securities-flow release already tracks "投資信託" (investment trust management companies) as one of its investor-type rows; `invtrust` was already in `XB_ALL_BUCKETS` for the combined stacked chart but had no standalone card like banks/trust_bank/trust_pension/life.

**Fix 1 — unhid the tab.** Removed `style="display:none"` + `data-hidden="redesign"` from the NISA nav button (`index.html` ~line 268). No other change to the tab's HTML/JS — it was already correctly built, just needed fresh data (see fetch-script fix below) and to stop being hidden.

**Fix 2 — Investment Trusts card added to Cross-border flows tab.** New `mbcard` (`cXb_invtrust`) added to the existing 4-card grid (banks/trust_bank/trust_pension/life), reusing the already-generic `xbStackedChart(canvasId, titleId, key)` function unchanged — just added `'invtrust'` to the two `.forEach` call-site arrays (`renderXb()` and `setXbWin()`) and to the grid's HTML. Zero new data pull — this is the same already-fetched MoF series, just a 5th standalone chart instead of only appearing as one segment of the combined `cXbAll` stacked bar. Card's note explains this is the closest direct FX read-through of NISA-driven household money (NISA purchases largely flow through toshin funds).

Verified live via `agent-browser`: NISA tab renders with real Dec-25/May-26 data (KPIs: ¥71tn cumulative, 28.26mn accounts, ¥1.27tn 10-firm May buys), Investment Trusts card renders a real, non-zero, mostly-equity-dominated stacked bar across the full history (2021-2026), zero console errors. Committed (`5da7220`).

## Top-20 fund flow chart moved here from the Japan NISA site (2026-08-11)

HanHan had this feature built on `japan-nisa-tracker`'s Monthly Pulse tab first (see that project's own AGENTS.md for the pipeline details — `fetch_nisa_fund_flows.py`, Wealth Advisor's monthly flash report, the `{code}` naming-convention gotcha), then asked "Can I have this in Japan dashboard instead of nisa dashboard" — moved, not duplicated. New full-width card in the **NISA tab**, right after "10-firm monthly gross buys": `#nisaFundFlowChart`, a horizontal bar chart of the top 20 funds by latest month's net inflow (¥bn), reading `nisa_fund_flows.json` (already mirrored into this project directory by the source script — no new fetch here, purely a display move). Removed the equivalent card + JS from `japan-nisa-tracker/index.html`.

**Two real Chart.js bugs found and fixed while wiring this up, both worth remembering for any future bar chart added to this dashboard:**
1. **Lazy-build the whole NISA tab's charts, like GPIF already does.** The NISA tab isn't the default-active tab, so its fetch (`nisa_data.json`/`nisa_fund_flows.json`) was resolving and calling `new Chart(...)` while the tab panel was still `display:none` — a canvas measured at creation time in a hidden container gets a 0-size box that this dashboard's generic post-activation `.resize()` (in `showTab()`) doesn't reliably recover, same root cause already documented in this file's own `initGpifChart()` comment. Refactored the whole NISA IIFE: the two `fetch()` calls now just cache their JSON (`nisaD`, `nisaFundFlowD`) and call a new `buildNisaTab()`/`buildNisaFundFlowChart()` pair only once, guarded by `window.__nisaChartsInit`; `showTab()` gained a `nisa` special-case (`window.__buildNisaTab`) mirroring the existing `gpif` one.
2. **`animation:false` was still separately required — lazy-build alone did not fix everything.** Even after the lazy-build refactor, the pre-existing `nisaMonChart` (10-firm monthly gross buys, `type:"bar"`, no animation override) was *still* rendering blank in headless screenshot verification, while the brand-new `nisaFundFlowChart` (also `type:"bar"`, built with `animation:false` from the start) rendered correctly immediately. Root cause: Chart.js's default ~1000ms entrance animation racing the headless screenshot capture, not a layout/visibility issue — confirmed on `japan-nisa-tracker`'s sibling `monChart` the same day, where `animation:false` alone (no lazy-build at all) was sufficient. Added `animation:false` to `nisaMonChart`'s options here too. **Takeaway: `animation:false` is the necessary fix for a bar chart rendering blank in an automated screenshot check; lazy-build-on-tab-activation is good practice for avoiding 0-size-canvas issues in general but is not a substitute for it.**

Verified live via `agent-browser` on the public URL: NISA tab's `nisaMonChart` and the new `nisaFundFlowChart` both render correctly (real bars, not blank) on first tab activation, zero console errors, rest of the tab (KPIs, cumulative/accounts line charts, quota doughnuts, target/allocation text, sources table) unaffected.

## Intervention tab: fixed stale/lagged USD/JPY data + HSBC-style before/after table + top-20 fund chart USD toggle (2026-08-11)

HanHan flagged, via a stack of Telegram messages: (1) the "USD/JPY level before & after each intervention round" card ("don't show this unless we have better jpy data"); (2) the Intervention episodes table's 1d/5d/20d effect columns should be replaced with an HSBC-style before/after level + % change; (3) confirmed the NISA tab's first two KPIs' "as of 2025" labeling was noticed/correct, not a bug; (4) asked for a USD toggle on the new top-20 fund flow chart; (5) asked whether Korea-style daily tracking is feasible for interventions (answered in chat, not built — see below).

**Root cause found for (1): `fetch_intervention.py` was still using raw FRED `DEXJPUS`** for both `long_history` (1985-present) and the `recent_history` that feeds `episode_snapshots` (the per-round before/after cards) — via a full FRED pull plus only a crude last-30-day Yahoo splice on top. `fetch_market.py`'s `fetch_fx()` had already been fixed on 2026-08-08 (see its own docstring) after finding FRED DEXJPUS runs several business days behind its FRED siblings, switching to Yahoo Finance `JPY=X` as the primary source — but that fix was never carried over to `fetch_intervention.py`, so the intervention snapshots were still built on the stale series that could misidentify the peak/trough day right around a fresh operation. **Fixed**: `fetch_intervention.py` now pulls the accurate `JPY=X` daily series (Yahoo, via `yahoo_jpy_history()`, same `yf.Ticker` pattern as `fetch_market.py`) for 1996-10-30 onward (the earliest Yahoo has), and FRED DEXJPUS only for the pre-1996 stretch where Yahoo has no data and staleness doesn't matter (Plaza Accord-era episodes). `long_history`/`recent_history` are now spliced FRED+Yahoo instead of FRED+crude-30-day-Yahoo-patch. Re-ran and verified: data now runs through the actual current day (was stuck at 2026-08-07, now 2026-08-11) and all 6 recent-round peak/trough snapshots (2022-09, 2022-10, 2024-04/05, 2024-07, 2026-04/05, 2026-07/08) show sane, specific levels and drops (e.g. the Jul30-31 2026 joint intervention: 163.86→157.53, -3.86%).

**Fix for (2): the "1d/5d/20d effect" columns were dead code — `ep.effects` was never populated anywhere** (grepped the whole file; no script or JS ever set it), so those 3 columns had shown "—" for every single row since the table was built. Replaced with 2 new columns, "USD/JPY before → after" and "% change", computed server-side in `fetch_intervention.py`'s new `episode_before_after()`: last close strictly before the episode's first operation date vs. the close ~5 trading days after its last operation date (deliberately simpler/different framing than the peak/trough search the snapshot cards above already do, so the two sections read as complementary, not redundant). Only computable for episodes with an exact `YYYY-MM-DD` start/end (10 of 20 episodes — mostly 2011+; older multi-month campaigns recorded as `YYYY-MM` correctly show "—", not a fabricated pair). `renderIvEpTable()` in `index.html` updated to match; colgroup rebalanced from 11 to 10 columns.

**Fix for (4):** added a ¥/$ toggle button pair to the top-20 fund flow chart's card header (`setNisaFundFlowCcy()`, global). USD conversion uses the latest USD/JPY spot from `market_data.json → fx → history` (fetched once when the NISA tab first builds, cached in `nisaFxRate`); chart is destroyed/rebuilt on toggle via `drawNisaFundFlowChart()`. No new data pull — same `nisa_fund_flows.json` rows, just divided by the spot rate when in USD mode.

**Investigated but not a bug, for (3):** the NISA tab's first two KPIs ("Cumulative buys, whole market" / "NISA accounts, whole market") really are stuck at Dec-2025 — confirmed live against FSA's own survey index page (`fsa.go.jp/policy/nisa2/survey/`): the Dec-2025 edition was published 2026-07-03 and is genuinely still the newest one FSA has issued (next edition, for Jun-2026, hasn't posted yet) — a ~7-month publication lag baked into FSA's own semi-annual cadence, not a fetch failure on this project's side. The KPI captions already disclose "· Dec 25" so the staleness is visible, not hidden.

**Answered in chat, not built, for (5) — "can we mimic Korea's daily tracking for interventions":** Japan's MOF does not publish anything analogous to Korea's near-daily flow releases for FX intervention specifically — MOF confirms individual operation days only via its quarterly day-by-day disclosure (released ~3 months in arrears, which is exactly why `RECENT_OPS`/`EPISODES` above are hand-curated from press estimates until each quarter's confirmation lands). The one genuinely daily, unrestricted proxy already on this dashboard is the USD/JPY spot line itself (now fixed to Yahoo `JPY=X`, live) — already what the long-run and snapshot charts plot day-by-day. If HanHan wants an actual new daily series, the closest real analogue would be BOJ's own daily current-account-balance data (published next business day, and already the basis several `RECENT_OPS` "market estimate" rows use to infer intervention days) — but that infers intervention indirectly from balance-sheet residuals, not a direct confirmed daily intervention amount, and isn't something MOF itself provides on a Korea-style daily cadence.

Verified live via `agent-browser`: Intervention tab's 6 snapshot cards render with real levels, "Intervention episodes" table's before→after/% change columns populate for the 10 exact-date episodes and show "—" for the 10 month-level ones, NISA tab's top-20 fund chart toggles cleanly between ¥bn and $bn with correct conversion, zero console errors throughout.

## Two quick layout tweaks from screenshot feedback + daily-tracking question corrected (2026-08-11, same day)

HanHan sent 2 photos with short instructions right after the above: (1) a screenshot of the newly-fixed "USD/JPY level before & after each intervention round" snapshot grid, captioned "This should be the first chart in [the Intervention tab's] program" (read literally as "NISA program" but the attached image is unambiguously the Intervention tab's own snapshot section, not anything on the NISA tab — treated as a slip while she was mid-conversation about NISA, not a real NISA-tab instruction); (2) the original Macrobond "Portfolio Investment Flows by Investment Trust Management Companies" screenshot (the same one that kicked off the whole NISA-tab-on-this-dashboard thread back on 2026-08-11 morning), captioned "You can remove this."

1. **Intervention tab reordered.** Moved the "USD/JPY level before & after each intervention round" card (`ivSnapGrid`) to be the very first card in `tab-intervention`, ahead of the IMF free-floating classification card and the Bessent-Japan diplomatic timeline — pure HTML reorder, no JS/data change.
2. **Investment Trusts card removed from the Cross-border flows tab.** This was the `xbTitle_invtrust`/`cXb_invtrust` `mbcard` added earlier the same day (2026-08-11) as a companion to the NISA-tab work, directly sourced from that Macrobond chart. Removed the card div from the `xbGrid`, dropped `'invtrust'` back out of the two `renderXb()`/`setXbWin()` `.forEach` call-site arrays (reverted to `['banks','trust_bank','trust_pension','life']`), and trimmed the investment-trusts sentence back out of the grid's shared note. **Left untouched, per this file's own earlier note**: `invtrust` still appears in `XB_ALL_BUCKETS` (the separate combined `cXbAll` stacked chart) and in the unrelated JGB-investor-purchases `show` array near line 2183 — both pre-date this session's NISA work and were never part of what HanHan asked to remove.

**Daily-tracking question, corrected.** In the batch of Telegram messages this session, "do you think it is possible to mimic the korea daily tracking... can we track these on a time series basis" got answered as if "these" meant FX interventions (the most recent topic at the time) — HanHan then clarified: **"I meant the 20 funds, rather than intervention."** Real answer for the top-20 NISA fund flow tracker: not really possible to go fully daily. The underlying source, Wealth Advisor's 投資信託への資金流出入速報, is itself a **monthly** flash report (published ~2-4 days into the following month) — there is no daily or weekly edition to switch to, so there's no equivalent of Korea's near-daily BOK/MOEF flow releases available for this specific series. The closest a "daily-ish" cadence gets for Japan fund flows generally: JSDA's own 10-firm survey (`fetch_nisa.py`, monthly) and the Investment Trusts Association's own AUM/net-asset data (not currently pulled) are both monthly at best; nothing public breaks out day-by-day flow at the individual-fund level the way Korea's SEIBRO/KOSCOM endpoints do for cross-border bond/equity flows. Flagged to HanHan rather than building something that can't actually be daily.

Verified live via `agent-browser`: Intervention tab shows the snapshot grid as the first card, zero console errors; Cross-border flows tab's `xbGrid` now has 4 cards (was 5), `xbTitle_invtrust` confirmed absent from the DOM, zero console errors.

**Correction, same day: HanHan actually meant delete the 6 snapshot charts entirely, not reorder them.** "This should be the first chart in NISA program" was misread as "move this section to be first" — her actual instruction was to delete the whole "USD/JPY level before & after each intervention round" section. Removed: the card's HTML (`ivSnapGrid`/`ivSnapStatus`), the `renderIvSnapshots(D)` call in the `intervention_data.json` fetch handler, the now-dead `renderIvSnapshots()` and `ivDropBracketPlugin()` JS functions, and the now-unused `#ivSnapGrid` responsive CSS rules. The Intervention tab's first card is now "IMF free-floating classification criterion" (the original pre-2026-08-11 order, before this whole thread started). **Left untouched**: `fetch_intervention.py`'s `build_episode_snapshots()` function and the `episode_snapshots` key it still writes to `intervention_data.json` — harmless unused data, no downstream consumer left after this removal, cheap to leave in case snapshots are wanted again later (just re-add the render call + HTML card). The FX-data-source fix from earlier the same day (Yahoo `JPY=X` instead of stale FRED) and the "before → after" episode-table columns are unaffected — those stay.

Verified live via `agent-browser`: Intervention tab's first card is now the IMF classification card, no trace of the snapshot grid anywhere on the page, zero console errors.

## Investment Trusts flow chart moved to the NISA tab (2026-08-11, same day)

Clarification of the earlier "You can remove this" instruction (Macrobond "Portfolio Investment Flows by Investment Trust Management Companies" screenshot) — HanHan re-sent the same screenshot with "Can you include this chart in NISA tab," i.e. she wanted it moved from the Cross-border flows tab to the NISA tab, not deleted outright. Re-added as a new full-width card on the NISA tab, right after the top-20 fund flow chart and before the Quota split grid: `<canvas id="cNisaInvTrust">`, title auto-set by the shared `xbStackedChart()` function (same one the Cross-border flows tab's other investor-type cards already use — no new chart-building code needed, just a new call site: `xbStackedChart('cNisaInvTrust', 'nisaInvTrustTitle', 'invtrust')` inside `buildNisaTab()`). Reuses `_xbData` (populated by the page-load-time `mof_flows.json` fetch, independent of which tab is active) — safe to call from the lazily-built NISA tab since that fetch always resolves well before any tab is manually activated. Same underlying MoF International Transactions in Securities series as before; **not** re-added to the Cross-border flows tab (stays removed there, per her original instruction on that specific location). Card's own note flags the real caveat: this dataset only goes back to 2021-04 (MoF's own release history), so it can't reproduce the full 2005-2026 range visible on a Bloomberg/Macrobond terminal — same data, shorter window.

Verified live via `agent-browser`: NISA tab renders the new stacked-bar-plus-net-line chart with real 2021-04–2026-07 data, zero console errors.

## Top 20 funds chart — wasted-space bug fixed (2026-08-12)

HanHan reported (repeatedly, incl. an earlier screenshot) "a lot of unwanted space" on the "Top 20 funds by net monthly inflow" chart. Root cause: the global `canvas{max-height:340px}` CSS rule was capping `#nisaFundFlowChart`'s actual rendered height at 340px while it sits inside a manually-set `<div style="height:640px">` wrapper — leaving ~300px of dead space below the bars. Fixed with a one-line id-scoped override: `#nisaFundFlowChart{max-height:640px}`. Verified by extracting the canvas directly via `canvas.toDataURL()` (not the page screenshot tool — see caveat below) — bars now fill the full card height.

Also investigated her "portfolio investment chart is not shown properly" complaint (the Investment Trusts chart, `cNisaInvTrust`). Spent a long diagnostic pass chasing what looked like a real partial-render bug (data/chart geometry always checked out correct via `Chart.getChart().getDatasetMeta()`, but `agent-browser screenshot` kept showing the chart cut off after ~10 of 64 months, or fully blank, inconsistently across many retries — tried resize()-on-next-frame, resize-loop-over-30-frames, full chart recreate, even a brand-new `<canvas>` element swap, none reliably fixed the *screenshot*). Eventually extracted the canvas directly with `canvas.toDataURL()` and decoded it to a real PNG — the chart was rendering **perfectly**, full 2021-04→2026-07 range, every time. **Conclusion: this was a screenshot-tool/compositor artifact specific to this sandboxed headless-browser environment (likely software-rendering canvas-layer capture timing), not a real code bug** — consistent with this exact chart having been verified fine via `agent-browser` when first built earlier the same overall thread (see the entry above). No code change made for this one. If HanHan is still seeing it broken in her actual browser after a hard refresh, that'd be a different, real issue worth a fresh screenshot to diagnose — but the underlying chart code/data is confirmed correct.

**Tooling caveat for future sessions**: don't trust `agent-browser screenshot` alone to verify complex/large canvas charts on this dashboard — cross-check with `document.getElementById(id).toDataURL('image/png')`, base64-decode it to a file, and view that directly when a screenshot looks suspiciously broken (partial/blank canvas) but the chart's data and Chart.js internal geometry (`getDatasetMeta()`) check out fine.

## Bottom-up NAV-flow proxy chart added to NISA tab (2026-08-12)

HanHan asked (separately, over Telegram) whether daily NAV-level fund flow could be estimated across the 18 scraped funds in `japan-nisa-tracker/fund_nav_daily/` (aggregating into an AUM-weighted composite index + estimated net flow), then confirmed "Yes add this to nisa tab in Japan dashboard" after being shown a chat-only chart preview, with a separate instruction to place it "in the back — only before quota split pie charts."

**New script**: `fetch_nisa_nav_flow.py` (this project) — re-implements the composite-index/flow logic already prototyped ad hoc in the conversation workspace (`nav_flow_analysis.py`), reading directly from `japan-nisa-tracker/fund_nav_daily/*.csv` (17 of 18 funds have both an AUM and reinvest-price column and are usable for the AUM-weighted composite; funds without an AUM column are excluded from the flow calc, not fabricated). Computes: AUM-weighted composite NAV index (chained daily reinvest-adjusted return), estimated daily net flow (AUM change minus what the price return alone implies), and its 20-trading-day rolling sum. Writes `nisa_nav_flow.json`, filtered to 2024-01-01 onward (the new-NISA era) for legibility — 635 points through 2026-08-10 as of this build.

**This is a static snapshot, not a live daily pull** — same caveat as the BOJ Balance Sheet draft tab's copied JSONs: the underlying `fund_nav_daily` CSVs are each one-off scraped from a different asset manager's site (see that folder's own README, 18 of 20 top funds captured, 2 blocked by site access) and are not on any automation. Re-run `fetch_nisa_nav_flow.py` after `japan-nisa-tracker/fund_nav_daily/` is refreshed to move `as_of` forward; not wired into `fetch_market.py`'s `SOURCES`.

**New card** on the NISA tab, `index.html`: 3 stacked charts (composite index line, daily-flow bar chart green/red by sign, 20d rolling-sum line) inside one full-width card, placed right after "Portfolio investment flows by investment trust management companies" and before the "Quota split / Within the growth quota" `grid2` — per HanHan's explicit ordering instruction. `buildNavFlowChart()` follows the same lazy-load pattern as `nisaFundFlowD`/`buildNisaFundFlowChart` (own `fetch('./nisa_nav_flow.json')`, called from `buildNisaTab()` once both the data and the tab-activation have resolved, in whichever order). Bar chart uses `animation:false` per this file's own earlier-established convention (see the Top-20 fund-flow-tracker entry in the root `japan-nisa-tracker/AGENTS.md` — Chart.js's default entrance animation races headless-screenshot capture and can render bars as blank/near-invisible).

Card's own note states the caveat explicitly (this is a modelled proxy, not a reported flow — distributions/fee-timing/FX-hedge rebalancing can look like flow on any single day) and cross-references the "Top 20 funds" and investment-trust charts above it as the same underlying household-flow story from different sources/frequencies.

Verified live via `agent-browser` on `localhost:8501` (the service's local port): all 3 charts render with real data (index ~248→~427, latest 20d rolling sum ¥1,107bn as of 2026-08-10, consistent with the chat-preview numbers HanHan already saw), zero console errors, quota-split pies confirmed still immediately following this card with no layout disruption.

## Top-20 fund flow snapshot → time series added (2026-08-12, same day)

HanHan sent a screenshot of the existing "Top 20 funds by net monthly inflow" bar chart (single-month ranking) and asked "Are we able to show this on a time series basis?" `nisa_fund_flows.json` already had 7 months on file (2025-12 through 2026-07, with the documented 2026-04 publication gap) — the individual top-20 *composition* rotates month to month, but 5 funds (eMAXIS Slim全世界株式, eMAXIS Slim米国株式(S&P500), インベスコ世界厳選株式, WCM世界成長株厳選, 楽天・プラス・オールカントリー) stayed in the top 20 in **every** one of the 7 months — the persistent core of NISA demand, as opposed to funds that rotate in/out.

New card added right after the existing snapshot chart (`nisaFundFlowChart`), before the investment-trust card: "Top NISA funds — net monthly inflow over time" (`nisaFundFlowTSChart`). Mixed bar+line Chart.js chart — gold semi-transparent bars are the full top-20 list's combined monthly net inflow (the total household-flow signal, ¥12.2–17.9bn/month range across the 7 months, no clear directional trend yet), overlaid with up to 6 colored lines for the funds with a complete 7-month history (persistent-core selection: `counts[fund] === months.length`, capped at top 6 by current rank if more qualify). No new data pull — pure client-side aggregation of the already-fetched `nisa_fund_flows.json`, computed in a new `buildNisaFundFlowTSChart(fd, months)` function called from the end of `buildNisaFundFlowChart()`. The 2026-04 gap is left as a real skipped label on the x-axis (7 categorical month labels, not 8 with a null slot) — consistent with this project's standing convention of never fabricating unpublished periods.

Verified live via `agent-browser` on `localhost:8501`: chart renders with the correct "Dec 25 – Jul 26" range pill, 6 datasets (5 fund lines + 1 total-bar) with legible legend, zero console errors. (Needed an explicit `location.reload()` in the verification session — `agent-browser open` on the same URL/hash the browser was already sitting on did not re-fetch the page, so the new DOM elements weren't visible until a hard reload; worth remembering for future same-session before/after checks on this dashboard.)

## Ground-truth confirmation line added: Investment Trusts Association of Japan (2026-08-12, same day)

HanHan asked "Let's run cross check. Total 20 fund flows - how's the size compared with the gross buy from top 10 companies and vs portfolio investment flow from trust management" — sizing table (5 overlapping months): Top-20 net flow runs 61-98% the size of 10-firm JSDA gross buys (same order of magnitude, consistent), while the MoF investment-trust series is 10-40x smaller and often opposite-signed (confirms it's measuring something structurally different — see the WGBI/foreign-KTB-style "different metric, not a range" pattern already established elsewhere in this workspace). She then asked "What will be a good confirmation data to cross check," and after being told about the Investment Trusts Association of Japan (投資信託協会)'s official monthly industry-wide net-flow stats, confirmed "Yes" to actually pulling it and adding it as a ground-truth line.

**New script**: `fetch_toushin_flows.py` — downloads `https://www.toushin.or.jp/tws/toukei_dw/I0112B_pub_m.xlsx` (their own B-1 "資産増減状況" monthly time-series file, found via the site's statistics index page, not a page scrape — the file is a direct, stable, unauthenticated download), reads the "株式 除ＥＴＦ" (Stock Investment Trusts, Excluding ETFs) sheet — the closest match to what NISA growth-quota equity funds actually are — and extracts column D "資金増減額/Amount of Assets Flows" (¥mn → ¥bn) plus total net assets, monthly, back to 2001-07. This is a genuinely different, independent, official primary source from all three other flow measures already on this tab (Wealth Advisor's top-20 flash report, JSDA's 10-firm survey, MoF's international-transactions-in-securities), and critically covers the **entire ~5,300-fund public stock-trust universe**, not a subset — the actual ground truth the other three are all partial views of. Writes `toushin_flows.json`, filtered to 2024-01+ for file size (full history available if ever wanted). ~1-month lag vs the Wealth Advisor top-20 series (latest as of this build: Jun-26 vs Jul-26).

**Sanity-checked before building anything**: Dec-25 industry-wide net inflow (¥1,454.8bn) sits almost exactly between the existing Top-20 estimate (¥1,217.1bn) and 10-firm gross buy (¥1,571.8bn) for that same month — independent confirmation that the other three series are measuring real, correctly-scaled things, not that any one of them was wrong.

**Added to `index.html`**: the industry-wide series is now a 7th dataset on the existing "Top NISA funds — net monthly inflow over time" chart (`nisaFundFlowTSChart`) — a thick white dashed line (`borderDash:[6,3]`, diamond point markers) drawn with `order:1` so Chart.js keeps it visually on top of the gold Top-20 bars and the 5 persistent-fund lines. New `toushinD` global + fetch block (`./toushin_flows.json`) wired into `buildNisaFundFlowTSChart(fd, months)`: looks up each chart month by exact date match against `toushinD.series`, `null` (not fabricated) for any month toushin hasn't published yet — same never-fabricate-unpublished-periods convention as everywhere else in this project. Handles both fetch-orderings (toushin resolving before or after `nisa_fund_flows.json`) by re-triggering the TS-chart rebuild from whichever fetch resolves second. Card's note text rewritten to explain what the new line is and why it sits above the gold bars (top 20 ≠ literally all NISA-driven demand).

Verified live via `agent-browser` on `localhost:8501` (after the now-standard `location.reload()` needed for same-session verification on this dashboard): all 7 datasets present in the legend, the white dashed line renders visibly above the gold bars across all 6 populated months (correctly stops at Jun-26, no Jul-26 point since toushin hasn't published that month yet), tracks the same up/down shape as the Top-20 total (Jan-26 spike, Feb/May dips) — visual confirmation the two independent sources move together, zero console errors.

## JGB holdings by holder type — new card, 2026-08-12

HanHan (Telegram): "Can you do a jgb holding by players chart" — this was already flagged as a known gap in this file's own review-doc notes ("JGB ownership by investor type (stock)... this dashboard has strong JSDA *flow* data but no *stock* picture").

**Source found:** BOJ Flow of Funds Accounts, Preliminary Figures (`sjpre.xlsx`, https://www.boj.or.jp/en/statistics/sj/sjpre.xlsx) — item "Db" (Central government securities and FILP bonds, = BOJ's own definition of "government bonds and treasury bills") broken out by holder sector on the assets side. BOJ does **not** keep a stable archived URL for past quarters — `sjpre.xlsx` always serves the latest quarter only — so this is a snapshot, not a time series; re-run the fetch script each quarter to roll it forward. (`sjlong.xlsx`, the "Time Series Table," only covers the last ~6 quarters and only for a handful of headline aggregates — not fine enough sub-sector detail for this chart, checked and ruled out.)

New `fetch_jgb_holders.py` — downloads `sjpre.xlsx`, walks 5 of its ~30 sheets (19/20 = all-sectors matrix, 21 = financial-institution sub-sectors incl. BOJ itself, 25/26 = insurance vs. pension-fund sub-split) to assemble a clean by-holder breakdown, writes `jgb_holders.json`. Categories: Bank of Japan, Banks, Insurance companies, Public pensions (incl. GPIF), Pension funds (corporate/other), Investment trusts, Overseas (foreign investors), Households, and an "Other" residual (nonfinancial corporations, general government's own holdings, local governments, PNPISH, other financial intermediaries — each individually small, ~1.7% combined). **Sanity-checked against independent reporting**: BOJ's own share computed here is 47.9% as of End of March 2026 — matches Nikkei Asia's contemporaneous headline ("BOJ's share of JGB holdings dips below 50% for the first time in 3.5 years," Mar 2026) almost exactly.

New card on the **Auctions & fiscal** tab (not the hidden Balance Sheet tab — this is market-structure/demand-side content, natural pairing with the existing "JGB gross issuance plan" card just above it, and needed to be visible without HanHan having to unhide anything), horizontal bar chart (`chart-jgb-holders`) sorted descending by size + 4 KPIs (total outstanding, BOJ/banks/foreign %). Verified live via `agent-browser` on `localhost:8501` and the public URL, zero console errors. Committed to git per the standing large-edit safety net.

**Not built (flagged, not a priority yet):** a true multi-year time series of the full by-holder breakdown — would require either scraping BOJ's Flow-of-Funds PDF archive quarter-by-quarter (tedious, ~50+ releases) or querying the BOJ Time-Series Data Search portal by series code (didn't find the exact FF series codes for this specific by-sector JGB-holdings cut in this session). The dashboard's existing "BOJ ownership share vs. free-float" chart (Balance Sheet tab, currently hidden) already covers the BOJ-only share over time from a different source — this new card is the complementary full-breakdown snapshot, not a duplicate.

## Top-20 fund flow chart — horizon toggle added (2026-08-12)

HanHan (Telegram): "For the investment trust, can I have toggle to select different horizon and also usd option?" — the "investment trust" card is the NISA tab's "Top 20 funds by net monthly inflow" chart (`nisaFundFlowChart`), sourced from Wealth Advisor's monthly flash report.

**USD already existed** — the JPY/¥ toggle shared between this snapshot chart and the "Top NISA funds over time" chart below it was built earlier (see `nisaFxRate`/`setNisaFundFlowCcy`) and still works; nothing to add there, just confirmed it live.

**New 1M/3M/6M/1Y horizon toggle**, since `nisa_fund_flows.json` already carries `flow_1m_100m_jpy`/`flow_3m_100m_jpy`/`flow_6m_100m_jpy`/`flow_1y_100m_jpy` per fund (straight from the source PDF's own 1mo/3mo/6mo/1yr columns) but the chart only ever plotted the 1-month figure. New button group next to the existing ¥/$ toggle in the card header; `setNisaFundFlowHorizon()` re-sorts the same fixed 20-fund universe by the selected window's flow value (not a new fund list — the universe stays "top-20-by-latest-1-month-inflow", same as before; switching horizon just re-ranks/re-sizes those same 20 bars, which the card's note now states explicitly to avoid the reader assuming it re-queries a different top-20). Title updates to match ("Top 20 funds by net monthly/3-month/6-month/1-year inflow"). Composes cleanly with the pre-existing USD toggle (verified 1Y+USD together). Verified live via `agent-browser`: chart re-sorts correctly on each horizon click, tooltip/axis labels update, zero console errors. Full-page `agent-browser` screenshots on this tab showed all canvases (old and new) as blank — re-confirmed via direct `Chart.getChart()` DOM query and a scrolled non-full-page screenshot that this is the same pre-existing "full-page screenshot doesn't reliably capture canvas content on this page" tool limitation already documented elsewhere in this file, not a real rendering bug.

## Daily market-data automation silently skipped 2 runs, fixed by manual trigger (2026-08-12)

HanHan (Telegram): "Market and bonds data not updated." Confirmed real — `market_data.json`'s `updated_utc` was stuck at `2026-08-10T16:02:20Z` and `git log` showed no `auto: BOJ data refresh` commit for 2026-08-11 or 2026-08-12, even though the "BOJ Hawk-Dove: daily data collection & dashboard refresh" automation (`eb8113b9-...`, daily 00:00 SGT) is `active=True` with a normal-looking `next_run`. Root cause of the skipped runs not diagnosed (no run logs available from here) — just fixed by running `python3.12 fetch_market.py` manually, which succeeded cleanly (0 source failures: jgb/fx/nikkei/us10y/call_rate/inflation/potential_growth/news all `[ok]`), then committed + pushed (`auto: BOJ data refresh 2026-08-12 (manual trigger...)`). `updated_utc` now `2026-08-12T11:59:13Z`, confirmed live on the public URL via `curl`. No service restart needed — `serve_nocache.py` reads `market_data.json` directly, no cache layer. JGB yield curve itself reads as-of 2026-08-10 even post-refresh — that's the source's normal 1-2 day publication lag, not a bug. Worth checking again in a day or two whether the automation's next scheduled run (2026-08-13 00:00 SGT) actually fires and produces a same-day commit — if it skips again, the automation itself likely needs debugging (e.g. the git-pull or Nomura-login steps upstream of `fetch_market.py` failing hard enough to abort the whole run despite instructions saying not to abort on those failures).

## Household assets chart converted to %; NISA fund-level domestic-vs-foreign classification built (2026-08-12)

HanHan asked to convert the household financial-assets-by-type chart (`chart-household-assets`) from ¥tn levels to % of total — the stacked-area chart now plots each category as a share of that quarter's total (y-axis 0-100%, tooltip shows both % and the underlying ¥tn), not a new data pull, just a client-side normalization of the same `household_assets.json` series. KPI row above it was already %-based, untouched.

Separately, after the JPM 19% vs. JSDA 41%-domestic-stock reconciliation discussion (see Telegram thread), HanHan asked to dig into domestic-vs-foreign equity **at the individual fund level**. Built a real classification, not an estimate: every one of the **111 distinct funds** that has appeared in the Wealth Advisor top-20 monthly-inflow list (`nisa_fund_flows.json`, Jul 2024–Jul 2026) was classified by its own stated mandate in its Japanese fund name (regex rules on keywords like 日本株/日経/TOPIX→domestic, 米国/S&P500/NASDAQ→US, 先進国株式→developed-ex-Japan, オールカントリー→global-all-country, etc.) into domestic-Japan equity / US equity / developed-ex-Japan equity / EM equity / global-all-country (~95% foreign, ~5% Japan) / global-mixed thematic / non-equity (bonds/gold/REIT/wrap). Zero of the 111 names were left unclassified — verified in both a Python prototype and the live JS port (same rule order, same regex, checked to produce identical category totals for the 2026-07 snapshot before shipping).

New card + chart on the NISA tab, right after "Top NISA funds — net monthly inflow over time": `buildNisaGeoChart()`, a 100%-stacked-area chart of the top-20 list's monthly flow by geography bucket (Domestic / Global all-country / Foreign (non-all-country) / Non-equity), computed client-side from the already-loaded `nisaFundFlowD` — no new fetch. KPI row shows the latest month's split. **Real, striking finding**: domestic-equity funds' share of top-20 flow was near-zero (0-9%) through most of 2024-2025, then rose to 20-23% by Mar/Jul 2026 — a genuine shift, not noise (confirmed by eyeballing the full 24-month series before charting, not just the endpoints).

**Explicitly flagged in the card's own note as a different universe from the JSDA 41% figure**, not a contradiction: Wealth Advisor only tracks investment-TRUST (fund) flows. NISA's growth-quota individual-stock purchases (~45% of total NISA money: 75% growth-quota share × 60% of that going to "stocks" not "funds", per `nisa_data.json → ten_firms_jsda.latest_allocation_2026Q1`) are invisible to this fund tracker entirely, and are mechanically ~100% domestic since NISA doesn't permit direct foreign-stock purchases — that's most of what drives JSDA's 41% figure. This chart answers the narrower, complementary question of "within just the fund-flow slice, how domestic vs. foreign" — and the answer there remains foreign-tilted (46% foreign + 29% global-all-country vs. 20% domestic in the latest month), while the domestic share has been climbing.

Verified live via `agent-browser`: `nisaGeoChart` renders with real percentages (20.2/29.1/45.8/4.9 in the latest month, matching the standalone Python classification exactly), KPI row populated, zero console errors, all other NISA-tab canvases (`nisaFundFlowChart`, `nisaFundFlowTSChart`, `cNisaInvTrust`) unaffected. Confirmed on the public URL via `curl`. Committed to git (2 commits: household-assets %-conversion, then the new geo-classification chart).

## Bug fix: NISA-tab "Investment trusts" seasonality chart was LT-bonds-only, not total (2026-08-13)

HanHan (Telegram): "The invest trust seasonality chart (LHS chart) seems wrong. The value is not total investment trust" → "I think that is long term bonds." Correct diagnosis. The chart (titled "Seasonality — total (equity + bonds)", left card in the NISA tab's `grid2` seasonality pair) was calling `xbFyChart(..., 'invtrust', 'seas')`, which reads `mof_flows.json`'s `seasonal`/`fytd` fields — and `fetch_mof_flows.py` only ever populates those from `monthb3.csv` (long-term bonds), per its own comment: "LT bonds only (unchanged legacy series) drives the seasonal / cumulative-FYTD tiles." Equity and short-term bonds were silently excluded from a chart explicitly titled "total."

Confirmed via live data pull: FY2026 Apr-Jul legacy LT-bonds-only values for `invtrust` were -0.01, 0.11, -0.12, -0.11 ¥tn (near-zero noise) — the true combined total (equity+ST+LT, from the already-correct `monthly_total` field) for the same months was 1.27, 0.78, 0.66, 1.20 ¥tn, almost entirely the equity leg (1.25, 0.62, 0.77, 1.24 ¥tn) that the old chart never plotted at all. Not a rounding/scale issue — the old chart was showing essentially a different, much smaller series.

**Fix:** added `xbTotalSeasChart()` (right after `xbInstrSeasChart()` in the `<script>` block) — builds the same Apr-Mar fiscal-year seasonality grouping client-side, but sums `monthly_total`/`monthly_total_usd` (already the correct equity+ST+LT combined series, used correctly elsewhere by `cNisaInvTrust`'s stacked chart) instead of the legacy LT-bonds-only field. Switched both `xbFyChart(...,'invtrust','seas')` call sites (initial `renderNisa()`-path load and the currency-toggle `setXbCur()` handler) to `xbTotalSeasChart(...,'invtrust')`.

**Scope check — nothing else affected.** The Cross-border-flows tab's own Seasonality/Cumulative-FYTD tiles (`total`, `trust_pension`, `life` buckets, via `xbFyChart(...,'seas'/'cum')`) still correctly use the legacy LT-bonds-only field — their card headers explicitly say "monthly net buying by fiscal year" / "Cumulative foreign bond buying" and their `.sub` notes say "net purchase of foreign long-term bonds" / "foreign long-term debt," not "total" — so those were already accurately labeled and are untouched. Only the NISA tab's `invtrust` chart claimed "total (equity + bonds)" while actually delivering bonds-only; that mismatch is what's fixed. `fetch_mof_flows.py` itself was NOT touched — the fix is entirely client-side, reusing already-correct fetched data, no re-fetch needed.

Verified live via `agent-browser` on localhost:8501: NISA tab → "Investment trusts" (left) seasonality chart now closely tracks "Equity — Investment trusts" (right) in shape/magnitude, as expected since equity dominates the true total — confirmed by directly comparing `_xbData.monthly_total.invtrust` (now used) against the old `_xbData.seasonal.invtrust` (still present in the JSON, just no longer read by this chart) via console. JS syntax checked clean (`node --check` on extracted script), `<div>`/`</div>` balanced (542/542) before/after, HTTP 200 on reload, zero console errors. Committed as `af7312d`.

## household_assets.json "discrepancy" investigated (2026-08-15) — not a bug, BOJ retroactively revised the whole series

While fact-checking a NISA report (see `japan-nisa-tracker`), household_assets.json's end-Dec-2025 total (¥2,394.08tn) looked wrong against March-2026 news coverage of BOJ's Q4 2025 Flow of Funds release (¥2,350.9tn, +5.3%/+¥118tn YoY, 48.5% cash, 21.6% equity+funds) — first flagged to HanHan as a suspected labeling bug in this file.

**It isn't a bug.** BOJ published an official retroactive revision (遡及改定) to its entire Flow of Funds series back to 2005 on 25 Jun 2026 (boj.or.jp/statistics/outline/notice_2026/not260525a.pdf — revises securities-company holdings, corporate/household equity and insurance/pension entitlement estimation methodology, among other line items). `fetch_household_assets.py` pulls live from BOJ's own Time-Series Data Search API, so it correctly reflects the post-revision series — it's *more current* than the March-2026 news figures, not wrong. Re-ran the fetch script fresh to confirm: output unchanged, confirming the file already reflected the revised series, not a stale/cached snapshot.

**Corrected current figures for end-Dec-2025** (post-revision, live from BOJ API): total ¥2,394.1tn (record high in the 1997–present series), +¥147.2tn/+6.6% YoY, cash & deposits 47.6% (a new record low — the full 1997-2026 series has no earlier quarter this low, so "record low," not just "lowest since Sep-2007" as pre-revision news framed it), equities+investment trusts 23.2% combined (also a fresh record high vs. the prior peak of 22.07% in Sep-2025). Used these to update the NISA v4 report's Section 7 (`Documents/Japan NISA/NISA_v4_Inheritance_Tax_Policy_Timeline_2026-08-15.pdf/.docx`).

**Implication for other dashboards/reports using this file or the same underlying BOJ series:** any prose/reports citing "¥2,350.9tn" or "48.5%"/"21.6%" for Dec-2025 household assets (sourced from pre-25-Jun-2026 news) are now stale, not wrong-at-the-time — worth a sweep if this comes up again elsewhere in the workspace. This dashboard's own `chart-household-assets` (NISA tab) pulls live from `household_assets.json` on every load, so it already reflects the revised series with no fix needed.
